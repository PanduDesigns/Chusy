# Chusy — Gestor de tareas del equipo

Gestor de tareas multiusuario con Kanban, lista, calendario con hitos,
vista personal "Mis tareas" (con recordatorios privados), etiquetas de
color, subtareas, descripciones con editor de texto enriquecido,
comentarios y enlaces adjuntos. Sitio
100% estático (HTML/CSS/JS, sin paso de compilación) pensado para vivir
en GitHub Pages, con Firebase como base de datos compartida en tiempo real.

Puedes cambiar el nombre "Chusy" por el que prefieras: aparece en
`index.html` (título de la pestaña y pantalla de login) y en
`js/components/sidebar.js`.

**Identidad visual:** el logotipo principal es el propio de Chusy
(`assets/chusy-badge.png`), con los colores de Martech Corporation como
base (gris pizarra #78848C, antracita #3C3C3C, dorado #FCD000 como
acento) — Martech aparece como crédito discreto en la pantalla de login y
al pie de la barra lateral, ya que Chusy es la marca del propio gestor.

**Modelo de acceso:** todo el equipo ve todos los proyectos y tareas de
proyecto — no hay privacidad entre compañeros ahí. La única excepción son
las tareas **personales** de "Mis tareas" (recordatorios propios sin
proyecto), que solo ven quien las creó y quien conste como responsable —
se pueden asignar a otra persona sin meterlas en ningún proyecto. Cualquiera
puede crear proyectos y tareas; borrar un proyecto (y sus tareas) o una
tarea de otra persona está limitado a quien lo creó o a un admin (constar
como responsable de una tarea personal no es suficiente para borrarla).

---

## 1. Puesta en marcha (una sola vez)

### 1.1 Crear el proyecto de Firebase
1. Ve a [console.firebase.google.com](https://console.firebase.google.com) y pulsa **Crear proyecto**.
2. El plan gratuito **Spark** es suficiente — no hace falta pasar a Blaze para nada de lo que usa esta app (los adjuntos son enlaces, no subidas de archivo).

### 1.2 Activar Authentication
**Compilación → Authentication → Comenzar** → pestaña **Sign-in method** → activa **Correo electrónico/contraseña**.

### 1.3 Activar Firestore Database
**Compilación → Firestore Database → Crear base de datos** → modo producción (ya tenemos reglas propias).

### 1.4 Registrar la app web y copiar la configuración
⚙️ **Configuración del proyecto** → "Tus apps" → icono **</>** (Web) → copia `firebaseConfig` en [`js/firebase-config.js`](js/firebase-config.js). No son datos secretos, puedes subirlos a GitHub sin problema.

### 1.5 Publicar las reglas de seguridad
**Firestore Database → Reglas** → pega el contenido de [`firestore.rules`](firestore.rules) → **Publicar**.

Cada vez que este archivo cambie entre una versión y otra del proyecto, hay que repetir este paso — subir el archivo nuevo no actualiza lo ya publicado en tu proyecto de Firebase, solo pegarlo de nuevo en la consola lo hace.

### 1.6 Probar en local
```bash
python3 -m http.server 8000
```
Y abre `http://localhost:8000` (Firebase Auth necesita `http://`, no sirve abrir el archivo con doble clic).

### 1.7 Subir a GitHub y activar GitHub Pages
Sube el contenido de esta carpeta a un repositorio y activa **Settings → Pages → Deploy from a branch → main /(root)**.

---

## 2. Primer uso

La **primera persona que se registre** se convierte automáticamente en **administradora**. El resto queda como **miembro**. Un admin puede ascender a otra persona editando su documento en `users/{uid}` desde la consola de Firebase (`role` → `"admin"`).

Para restringir quién puede registrarse a los correos de tu empresa: Firestore → colección `meta` → documento `config` → campo `allowedEmailDomains` (array), p. ej. `["martechcorp.com"]`.

---

## 3. Cómo funciona lo nuevo de esta versión

### Una sola ventana para crear y editar tareas
Se ve todo el formulario de golpe (fechas, prioridad, responsables, etiquetas, subtareas, adjuntos…) tanto al crear como al editar, y **nada se guarda hasta pulsar "Aceptar"**. Si cierras sin aceptar habiendo cambiado algo, pregunta si quieres descartarlo. Los comentarios son la única excepción: se envían al momento en cuanto pulsas "Enviar", y solo están disponibles editando una tarea ya guardada (una tarea nueva todavía no tiene dónde colgarlos).

### Clic derecho para más acciones
Clic derecho sobre una tarea (en Lista, Tablero o Mis tareas): marcar completada, duplicar, convertir en hito, abrir detalles o eliminar. Clic derecho sobre un proyecto en la barra lateral: editar (nombre, icono y color), archivar o eliminar (esto último borra también todas sus tareas y comentarios).

### Icono y color de proyecto
Cada proyecto se identifica con un emoji "sombreado" del color que elijas, en vez del simple punto de color de antes. Al crear o editar un proyecto hay una rejilla de emojis pensados para procesos industriales (fábrica, engranaje, tornillería, grúa, tronco de madera, coche, tren, avión, barco, componentes eléctricos…), un desplegable "Más iconos" con otra tanda más variada, y un campo para pegar o escribir cualquier otro emoji, así que las combinaciones posibles son prácticamente ilimitadas. Ese icono aparece en la barra lateral, el archivo, el buscador, los filtros, Mis tareas, la cabecera del proyecto y la línea de tiempo global, para diferenciar los proyectos de un vistazo.

### Etiquetas de color
Al escribir una etiqueta en una tarea, se sugieren las que ya existen (con su color) para reutilizarlas; si escribes una nueva, puedes elegirle color desde una paleta, y se queda seleccionado ese mismo color por defecto para la siguiente etiqueta nueva que crees. El color de una etiqueta es compartido: cambiarlo afecta a todas las tareas que la llevan.

### Mis tareas: recordatorios personales
El botón "+ Tarea personal" en "Mis tareas" crea una tarea sin proyecto, visible por defecto solo para quien la crea — para apuntes y recordatorios propios. Se distinguen con la etiqueta "🔒 Personal". Se le pueden asignar responsables igual que a cualquier otra tarea (ver "Tareas en varios proyectos a la vez" más abajo): en cuanto tiene a alguien más como responsable, esa persona también puede verla y editarla desde su propia "Mis tareas", aunque no la haya creado ella.

### Tareas en varios proyectos a la vez
Desde el propio detalle de una tarea, el campo "Proyectos" deja añadirla a más de un proyecto — cada uno con su propio desplegable de sección (una tarea puede estar en "Diseño" dentro de un proyecto y en "Pendiente de revisar" dentro de otro, a la vez), igual que en Asana. Se puede hacer con cualquier tarea, incluidas las personales de "Mis tareas": desde ahí mismo se puede crear una tarea, asignarla a otra persona y meterla en un proyecto (o en varios) sin cambiar de ventana. Quitar el último proyecto de una tarea no la deja huérfana: si tenía dueño/a (se creó como personal en algún momento), vuelve a serlo suyo; si no, pasa a ser personal de quien esté editándola en ese momento.

### Mis tareas agrupada por urgencia de fecha
Igual que la vista personal de Asana, las tareas de "Mis tareas" se agrupan en bloques según su fecha límite: **Vencidas**, **Hoy**, **Mañana**, **Próximos 7 días**, **Más adelante** y **Sin fecha** — cada bloque es una sección plegable, como las de un proyecto. Una tarea vencida que ya está completada aparece en "Hoy" en vez de en "Vencidas", para no acumular ahí ruido de cosas que ya no hace falta mirar. Dentro de cada bloque, las tareas siguen el orden de columna que tengas elegido (o el orden por defecto, fecha y luego prioridad, si no has pulsado ninguna).

### Línea de tiempo / Gantt
Dos formas de verla: por proyecto (pestaña "Línea de tiempo" junto a Lista/Tablero/Calendario, agrupada por sección) o global para todo el departamento (botón fijo en la barra lateral, agrupada por proyecto). Una tarea con fecha de inicio y fecha límite se dibuja como una barra que cubre toda su duración; con una sola fecha, como un bloque de un día; los hitos siempre como un rombo. La línea vertical dorada marca el día de hoy.

### Calendario: duración completa, no solo el vencimiento
Igual que en la línea de tiempo, una tarea con inicio y fin se dibuja como una barra que ocupa todos los días entre medias (incluso cruzando de una semana a la siguiente), no solo un punto en la fecha límite.

### Línea de tiempo: zoom por días, semanas o meses
Botones "Días / Semanas / Meses" en la propia línea de tiempo (por proyecto y global). En semanas, cada columna muestra su número de semana ISO del año (S29, S30…).

### Exportar la línea de tiempo a Excel o PDF
Botón "⬇ Exportar" en la barra de herramientas de cualquier línea de tiempo (por proyecto y global), con dos opciones. Exporta exactamente lo que hay filtrado/visible en pantalla en ese momento — no una consulta aparte.

El **Excel de la línea de tiempo de UN proyecto** reproduce la plantilla real de la empresa (cabecera CLIENTE/PROYECTO/FECHA, un color de barra por sección, el mismo formato condicional de siempre — ver el historial de la v38 para el detalle de cómo se rellena, y por qué antes no se coloreaba ninguna barra). Si los datos no caben en la plantilla (más de 50 tareas con fecha, más de 40 semanas de rango, o más de un año natural), cae solo a un Excel genérico avisando por qué, sin dejar nunca sin archivo a quien exporta; la **línea de tiempo global** (varios proyectos a la vez) usa siempre este genérico, porque la plantilla da por hecho un único CLIENTE/PROYECTO. El genérico es una fila por tarea (sección/proyecto, responsables, prioridad, fechas, días, estado) con una columna de "cronograma" en texto a escala del rango completo, para tener también un vistazo visual sin salir de la hoja.

El **PDF** (en los dos casos) es un Gantt de verdad, apaisado, con barras de color por prioridad y paginación automática si hay muchas tareas. Las librerías que hacen falta se cargan solas la primera vez que se usa cada opción — no alargan la carga de la app para quien no exporta nunca nada.

### Notificaciones: aviso al asignarte una tarea
Campana 🔔 fija arriba a la derecha, visible en cualquier pantalla de la app. Se enciende con un punto cuando hay alguna notificación sin leer; al pulsarla, un panel con las últimas 20 (más que eso no se llegan ni a acumular, ver abajo), cada una clicable para abrir la tarea directamente (y marcarla como leída de paso), con un botón para marcarlas todas como leídas de golpe. Las que quedan por leer se distinguen a simple vista (fondo teñido y texto a más contraste, no solo el puntito), y cada una dice también el proyecto de la tarea, si tiene uno. Si te asignan algo con la app ya abierta, además del punto en la campana sale un aviso emergente más llamativo (icono, dos líneas, clicable, más tiempo en pantalla que un toast normal) — uno agrupado si llegan varias de golpe. De momento solo un tipo de aviso: que alguien te asigne una tarea (desde el propio detalle de la tarea, o desde las acciones de asignar en la selección múltiple) — nunca te avisa a ti mismo/a de algo que has hecho tú, nunca a un usuario ficticio del importador de Asana (no hay nadie detrás que lo lea), y solo cuenta como "nuevo" a quien no fuera ya responsable de esa tarea en concreto, para no avisar de nuevo por simplemente volver a guardar una tarea sin cambiar quién la lleva. Desde la v39 la colección se autolimpia sola por persona (máximo 20: las sin leer más antiguas de ese exceso se marcan leídas solas, y las leídas más antiguas se acaban borrando) — ver su historial para el mecanismo exacto.

### Filtros, incluidas etiquetas y campos personalizados
Barra de filtros encima de Lista/Tablero/Calendario/Línea de tiempo/Mis tareas: Responsable, Prioridad, Estado, Etiquetas y cualquier campo personalizado del proyecto. Se combinan entre sí y se aplican al momento, sin botón de confirmar.

**Campos personalizados** (por ejemplo "Cliente" con opciones "Talgo, Stelia, Togg"): clic derecho sobre un proyecto en la barra lateral → "Campos personalizados". Una vez creados aparecen como desplegable en cada tarea de ese proyecto y como filtro más.

### Buscador por texto en la barra de filtros
Junto a los filtros de chips, un cuadro de texto que filtra por título y por el contenido de la descripción — sin distinguir mayúsculas ni acentos ("diseno" encuentra "Diseño"). Se aplica al momento, según se escribe, y se combina con cualquier otro filtro que tengas activo. Disponible en Lista, Tablero, Calendario, Línea de tiempo (por proyecto y global) y Mis tareas.

### Sin parpadeo al abrir la app con sesión iniciada
Antes se veía un instante la pantalla de login incluso con la sesión ya iniciada, mientras Firebase comprobaba si había cuenta. Ahora se muestra una pantalla de carga mínima hasta saber con certeza si hay sesión o no, y solo entonces aparece la pantalla que corresponda.

### Al entrar, directo a la última sección visitada
Si la última vez estabas en un proyecto, vuelves a ese mismo proyecto (y a la misma vista dentro de él: Lista, Tablero, Calendario o Línea de tiempo); si estabas en Mis tareas, la Línea de tiempo global o el Archivo, vuelves ahí. La primera vez que entras en un navegador, o si el proyecto recordado ya no existe (se borró o se archivó), el destino por defecto es "Mis tareas". Es una preferencia de este navegador, no de la cuenta — mismo criterio que la barra lateral minimizable y los filtros de Mis tareas (ver apartado 7).

### Columnas ordenables, redimensionables, reordenables y ocultables en Lista y Mis tareas
Nombre, Fecha límite, Responsables, Prioridad, Etiquetas y cada campo personalizado se pueden pulsar para ordenar — alfabético, por fecha, por prioridad o por valor — con flecha indicando la dirección; un segundo clic invierte el orden. Las tareas completadas siempre van al final. Sin pulsar ninguna columna, el orden por defecto es fecha límite ascendente (sin fecha al final) y, dentro de la misma fecha, por prioridad.

Cada columna se puede además **redimensionar** (arrastrando su borde derecho), **reordenar** (arrastrando su título y soltándolo sobre otra columna, antes o después según de qué lado se suelte) y **ocultar/mostrar** desde el botón "☰ Columnas" de la barra de herramientas — la de Nombre no se puede ocultar ni arrastrar, siempre es la primera. Ancho y ocultas son preferencias de cada persona *por sitio* (un proyecto concreto, o "Mis tareas" — cambiar una no afecta a la otra); el **orden en cambio es único para toda la persona**: reordenar columnas en Mis tareas o en cualquier proyecto cambia ese mismo orden en todos los sitios donde esas columnas existan. Nada de esto cambia lo que ve nadie más del equipo. Si entre anchos y columnas la tabla no cabe en la pantalla, aparece scroll horizontal en vez de aplastar el contenido. Esa cabecera (nombres de columna, con sus botones de ordenar/redimensionar/arrastrar) se queda siempre fija arriba, junto con la barra de herramientas — al desplazarte por una lista larga, solo se mueven las filas y las secciones, nunca la cabecera.

El **orden de filas** (qué columna y en qué dirección — no confundir con el orden de columnas del párrafo anterior) también se recuerda ahora, por persona y *por sitio* igual que el ancho y las columnas ocultas: la próxima vez que entres a ese proyecto o a Mis tareas, la tabla ya sale ordenada tal y como la dejaste, sin tener que volver a pulsar la columna. Viaja entre dispositivos porque vive en tu cuenta, no en este navegador.

### Etiquetas con columna propia
Antes las etiquetas de una tarea aparecían pegadas al nombre, sin columna propia (hasta 2, el resto se recortaban) y sin poder ordenar por ellas. Ahora tienen su propia columna "Etiquetas" en Lista y Mis tareas, con las mismas capacidades que cualquier otra: ordenar (por la que sea alfabéticamente primera, si una tarea lleva varias), redimensionar, ocultar/mostrar y reordenar. Si ocultas esta columna, las etiquetas de una tarea dejan de verse en la tabla — siguen intactas y editables desde el detalle de la tarea.

### Campos personalizados también en Mis tareas
Además de los de un proyecto (clic derecho sobre el proyecto → "Campos personalizados", o el botón "+ Campo personalizado" en la barra de herramientas de Lista), ahora "Mis tareas" tiene su propio botón "+ Campo personalizado": son campos tuyos, para clasificar como quieras cualquier tarea que veas ahí (sea de un proyecto o un recordatorio personal), sin que le aparezcan a nadie más. Al abrir una tarea que tiene campos personales rellenables, se marcan con "· personal" para distinguirlos de los del proyecto.

### Tres tipos de campo personalizado
Lista de opciones (como antes), número y texto libre — se elige al crear el campo. Los tres se pueden usar como columna y como filtro.

### Buscador global (⌘K / Ctrl+K)
Botón "Buscar…" arriba de la barra lateral, o el atajo de teclado desde cualquier pantalla. Busca en tareas, proyectos y personas (categorías activables/desactivables), y trae unas "búsquedas guardadas" rápidas: tareas que has creado, que has asignado a otros, y completadas recientemente.

### Archivo
Clic derecho sobre un proyecto → "Archivar proyecto": desaparece de la lista principal pero no se borra. Se consulta desde "Archivo" en la barra lateral; clic derecho sobre un proyecto archivado ahí da las dos acciones que le faltaban: "Desarchivar proyecto" (vuelve a la lista activa) y "Eliminar proyecto" (borra también todas sus tareas y comentarios, con confirmación de por medio — no se puede deshacer).

### Vacaciones inhábiles en la línea de tiempo
Botón "🏖️ Vacaciones inhábiles" en la línea de tiempo (por proyecto y global): sombrea en rojo agosto completo y del 22 de diciembre al 6 de enero, en cualquier nivel de zoom. Son fechas por defecto — si el cierre real de la empresa es distinto, dímelo y las ajusto.

### Corrección: las barras ya no tapan el nombre de la tarea
En la línea de tiempo, la columna de nombres (fija a la izquierda al desplazar) tenía la misma prioridad de apilado que las barras, así que una barra larga podía pintarse encima del texto. Ahora el nombre siempre queda por delante.

### Mi cuenta y administración de equipo
Clic en tu nombre (pie de la barra lateral) abre un menú con **"Mi cuenta"**: ver tu correo, tu rol y desde cuándo eres miembro, cambiar tu nombre visible y cambiar tu contraseña (pide la contraseña actual). Si tu cuenta es admin, ese mismo menú añade **"Administrar equipo"**: lista de todas las cuentas registradas con un desplegable para cambiar el rol de cada una (no te puedes quitar el admin a ti mismo si eres la única persona administradora), y la configuración de qué dominios de correo pueden registrarse — lo que antes solo se podía tocar desde la consola de Firebase.

### Eliminar (o reactivar) una cuenta
Desde "Administrar equipo", cada cuenta registrada tiene un botón 🗑 — pensado para cuando alguien deja el equipo. Pierde el acceso a Chusy al momento, incluso si tenía la sesión abierta en ese preciso instante, pero la cuenta no se borra de verdad: pasa a una sección aparte, "Cuentas eliminadas", con un botón ↩ para **reactivarla** en cualquier momento (por ejemplo, si se elimina a quien no tocaba por error) — instantáneo, sin perder nada. Mientras está eliminada se comporta como un usuario ficticio del importador de Asana: sus tareas y comentarios pasados se siguen viendo con normalidad, marcada con «· Eliminado» donde ya estuviera asignada, pero no se ofrece como opción al asignar tareas nuevas — así se puede reasignar rápido su carga de trabajo a otra persona con las herramientas de siempre (el detalle de cada tarea, o "Asignar a una persona" en la selección múltiple). No puedes eliminar tu propia cuenta desde aquí.

### Importador de Asana (solo administradores)
Desde el menú de tu nombre (pie de la barra lateral), si eres admin verás también **"Importar desde Asana"** — no aparece para el resto del equipo, así que no hace falta ocultarlo de ningún otro sitio. Carga ahí el archivo `.json` de export (formato `asana-api-export`) y verás un resumen de qué trae y cuánto es nuevo frente a lo ya importado antes; se puede cargar el mismo archivo varias veces sin miedo a duplicar nada.

Reimportar el mismo archivo (o una versión más reciente de un proyecto que ya existía) es seguro y autorreparador: si trae una sección que el proyecto en Chusy todavía no tiene, se añade sola, y las tareas que se hubieran quedado apuntando a una sección que ya no existía se corrigen — esto podía pasar al reimportar un proyecto existente, y hacía que esas tareas «desaparecieran» de Lista o Tablero aunque siguieran contando en el total. Por eso el botón de confirmar sigue activo aunque el resumen diga «nada nuevo»: repasar y reparar sigue siendo útil aun sin crear nada. "Borrar todo lo importado" también limpia el archivo que tuvieras cargado en el panel y su tabla de equivalencias, para no dejar en pantalla a nadie cuyo perfil ficticio se acaba de borrar.

Como las personas de Asana y las cuentas de Chusy no son las mismas, cada persona de Asana que aparece en el archivo se lista con un desplegable para decir "esto es en realidad Fulanito" (solo se pueden elegir cuentas ya registradas en Chusy — no hay forma de crear cuentas reales desde aquí). Mientras no se le asigne una cuenta real, queda como **usuario ficticio**: sus tareas y comentarios se ven con normalidad, pero no puede entrar en Chusy ni se ofrece como opción al asignar tareas nuevas. En cuanto se aplica una equivalencia, se reescriben automáticamente todas sus tareas y comentarios ya importados con la cuenta real — se puede hacer en el momento de importar o más adelante, según se vaya registrando cada persona; esta tabla de equivalencias está siempre disponible en el mismo panel, no solo durante una importación.

Las subtareas de Asana entran como tareas normales del proyecto (con su responsable, fechas y comentarios propios si los tenían), no como el checklist ligero de "subtareas" de Chusy — se nota porque su descripción empieza con una referencia de vuelta a la tarea de la que venían. Los adjuntos **no se importan**: el export no trae los archivos, y reconstruir enlaces a partir del historial de actividad de Asana solo serviría mientras se mantenga acceso a Asana, lo cual no tiene sentido para una migración que busca dejar de depender de ella. Hay también una "zona de riesgo" en el propio panel para borrar de un tirón todo lo importado (por si una prueba sale mal) sin tocar nada creado a mano.

### He olvidado mi contraseña
Enlace bajo el campo de contraseña en la pantalla de entrada: pide el correo y envía un enlace de Firebase para elegir una contraseña nueva. Por privacidad, el mensaje de confirmación es el mismo exista o no una cuenta con ese correo.

### Barra lateral minimizable
Botón redondo en el borde derecho de la barra lateral (junto al logo): la reduce a una franja estrecha con solo el icono, la lista de proyectos como puntos de color y los iconos de Mis tareas / Línea de tiempo / Archivo — pasa el ratón por encima de cualquier icono para ver su nombre. Se anima con una transición suave. Es una preferencia de este navegador (se guarda con `localStorage`, no en tu cuenta), así que no afecta a otras sesiones ni dispositivos. En pantallas de móvil, donde la barra lateral ya se abre y cierra como un panel superpuesto con el botón de menú, este control no aparece — ahí no hace falta.

### Selección múltiple y edición masiva (Lista y Mis tareas)
Ctrl/Cmd+clic sobre una tarea la añade o la quita de la selección sin abrir su detalle; Shift+clic selecciona todo el tramo desde la última tocada (puede cruzar secciones, o los grupos de fecha en Mis tareas). Un clic normal sigue abriendo la tarea como siempre. Con algo seleccionado aparece una barra flotante abajo: mover a otra sección, cambiar de proyecto (traslado completo: deja la tarea en ese proyecto y en ninguno más, quitando cualquier proyecto adicional que tuviera — para añadir un proyecto sin perder los que ya tenía, se usa el selector de proyectos del propio detalle de la tarea; ajusta la sección al primero del proyecto destino), asignar a una persona, establecer fecha de inicio y/o límite (cada una con su propia casilla — dejarla en blanco la borra), eliminar, y un menú "···" con marcar como completadas / sin finalizar, agregar colaboradores (sin quitar a los que ya tuviera cada tarea), combinar tareas duplicadas en una sola (une responsables, etiquetas, subtareas, adjuntos y comentarios en la que elijas conservar, y borra las demás), convertir en hitos, y mover a mis tareas (las saca del proyecto y las deja como tareas personales de quien pulsa el botón — no de quien las tuviera asignadas antes —, sustituyendo los responsables anteriores por esa persona, ya que a partir de ahí solo ella podrá verlas). Disponible tanto en la vista de Lista de un proyecto como en Mis tareas; ahí la selección puede mezclar tareas de varios proyectos distintos y recordatorios personales a la vez, así que "mover a otra sección" no aparece (las secciones son de un proyecto concreto) y "cambiar de proyecto" ofrece todos los proyectos sin excluir ninguno. Sigue faltando en Tablero.

### Corrección: "Mis tareas" ya muestra las tareas de todo el equipo, no solo las de un admin
Solo quien tenía rol de admin veía algo en "Mis tareas" — al resto del equipo le aparecía siempre vacía, por más tareas que tuviera asignadas. La causa estaba en la consulta a Firestore: pedía las tareas con un único `where("assigneeIds","array-contains", uid)`, sin acotar por `projectId` ni `ownerId`. La regla de lectura de tareas es un OR de esos dos campos (más "eres admin"), y Firestore necesita poder demostrar que una consulta cumple la regla a partir de sus propios filtros; como esta no acotaba ninguno de los dos, la rechazaba entera para cualquiera que no fuera admin — la rama "eres admin" era la única que se salvaba siempre, y por eso solo un admin veía resultados. Ahora son dos consultas separadas por debajo (una acotada a `projectId != null`, otra a `ownerId == uid`), cada una calcada a una rama del OR, así que las dos quedan verificables para cualquiera con sesión iniciada — de cara a quien usa la app sigue siendo una sola vista, sin ningún cambio visible más que, ahora sí, ver lo que le corresponde. La consulta de proyecto es nueva para Firestore, así que es fácil que la primera vez que se ejecute pida crear su índice compuesto (el aviso con enlace de siempre, ver el apartado 7).

### Responsables: solo cuentas reales de Chusy
El selector de responsables de una tarea ya no ofrece las cuentas ficticias del importador de Asana — solo se pueden elegir personas con cuenta real. Si una tarea ya tenía asignado alguien ficticio (a quien todavía no se le ha aplicado una equivalencia), esa persona concreta se sigue viendo ahí, marcada con «· Asana», para que sepas que sigue pendiente; el resto de fichas ficticias no aparecen como opción. Mismo criterio en el selector de "asignar a una persona" y en "agregar colaboradores" de la barra de selección múltiple, aunque ahí (al afectar a varias tareas a la vez) no hay excepción posible: no se ofrece ninguna cuenta ficticia. Desde la v35, una cuenta eliminada (ver más arriba) sigue exactamente este mismo criterio, con «· Eliminado» en vez de «· Asana».

### Eliminado el campo "Bloqueada por"
Ocupaba mucho espacio en el modal de tarea y apenas se usaba. El dato (`dependsOn`) sigue existiendo en Firestore para las tareas que ya lo tuvieran (y "Combinar tareas duplicadas" lo sigue uniendo entre las tareas fusionadas), pero ya no hay ninguna forma de verlo ni editarlo desde la interfaz.

### Descripción: editor de texto enriquecido y campo más grande
El campo Descripción es ahora mucho más grande (antes apenas cabían un par de líneas) y tiene un editor de texto enriquecido — barra de herramientas y menú de «/» al estilo Asana, con negrita, cursiva, subrayado, tachado, listas con viñetas y numeradas, cita, enlaces, código en línea, bloque de código y títulos (H1/H2/H3). Las descripciones de tareas antiguas (texto plano, incluidas las importadas de Asana) se siguen viendo con normalidad — se van convirtiendo al nuevo formato en cuanto se editan y guardan de nuevo. Todo el HTML que produce este editor se limpia (se sanea) antes de guardarse en Firestore, quitando cualquier etiqueta o atributo que no sea de puro formato de texto — cualquiera del equipo puede editar la descripción de cualquier tarea de proyecto, así que no basta con confiar en que el editor de turno solo genere marcado "bueno".

### Filtros de "Mis tareas" recordados, con las pendientes visibles de serie
Los filtros que apliques en "Mis tareas" ahora se recuerdan: quedan guardados en este navegador (no en la cuenta — mismo criterio que la barra lateral minimizable, así que en un ordenador compartido cada cuenta guarda los suyos por separado) y se recuperan la próxima vez que entres, en esta sesión o en otra. La primera vez que alguien entra no hay nada guardado todavía, así que por defecto se aplica el filtro "Pendiente" (se ocultan las completadas), para no enterrar la vista bajo todo lo que ya está hecho; en cuanto cambies los filtros a tu gusto, eso es lo que queda recordado a partir de ahí. Cuando el filtro está ocultando algo, el contador de arriba pasa a decir "X de Y tareas" en vez de solo el total.

### Modo claro, oscuro o clásico, a elegir
Desde "Mi cuenta" (pie de la barra lateral) hay una sección "Apariencia" con tres opciones: 🌙 Oscuro (el de siempre, y el que se aplica de serie si no has elegido nada todavía), ☀️ Claro y 🟧 Clásico (fondo claro con la barra lateral oscura y el acento en coral, los colores de Asana). Se aplica al momento, sin cerrar el modal, y queda guardado en tu cuenta — si entras desde otro dispositivo, se respeta lo último que hayas elegido ahí.

### Recompensa al completar tareas: confeti y rachas
Al marcar una tarea sale un chispazo discreto de confeti desde el propio círculo. Además, la recompensa "grande" (confeti cayendo por toda la pantalla + un mensaje) salta en tres situaciones: al completar varias de golpe con la selección múltiple ("Marcar como completadas" en el menú "···"), al completar 3 o más **seguidas y rápido** marcando el círculo una a una — sin usar la selección múltiple —, y al ir sumando completadas a lo largo del **mismo día** y cruzar uno de los hitos (5, 10, 15, 20, 30, 50, 75, 100 — no hace falta que sean seguidas ni rápidas). Cada situación tiene su propio emoji y sus propias frases. Respeta `prefers-reduced-motion`: si el sistema lo pide, no se dispara ninguna animación (la tarea se completa igual).

### Panel de métricas (solo administradores)
Nuevo botón "📊 Métricas" en la barra lateral, fijo junto a Mis tareas/Línea de tiempo/Archivo, visible solo para cuentas con rol admin. Muestra, de todos los proyectos a la vez: tarjetas de resumen (total, completadas con su %, pendientes, vencidas), carga por persona (tareas activas asignadas a cada quien ahora mismo, como barras ordenadas de más a menos) y el listado de tareas vencidas (con proyecto, responsables y días de retraso, cada una clicable para abrirla directamente). Se actualiza solo, en tiempo real, igual que el resto de la app. A propósito no cuenta las tareas personales de "Mis tareas" de cada quien — son privadas por diseño, ni un admin las ve todas de golpe sin que se las asignen expresamente.

### Negrita en parte del título de una tarea
El campo de título del modal de tarea tiene ahora un botón "B" (también Ctrl/Cmd+B): selecciona parte del texto y púlsalo para ponerla en negrita — un segundo toque sobre el mismo trozo la quita. Debajo del campo aparece una vista previa de cómo se verá, visible solo mientras el título tenga algo en negrita. Se guarda como texto normal con el trozo marcado entre `**dobles asteriscos**` (no hace falta escribirlos a mano, el botón lo hace por ti), y así se ve — en negrita, no los asteriscos — en Lista, Tablero, Calendario, Línea de tiempo, Mis tareas, el buscador global y el panel de métricas. Para ordenar, buscar o exportar (Excel, PDF) se usa siempre el texto sin las marcas, así que la negrita no afecta a ninguna de esas tres cosas.

### Historial de cambios: v18 → v40

Este proyecto se entrega como ZIPs numerados (`Chusy-18.zip`, `Chusy-19.zip`...); cada número es una entrega completa, no un parche — el ZIP más reciente es siempre la fuente de la verdad, no hay que combinar varios. Este apartado resume qué cambió en cada salto desde la v18, para quien retome el proyecto desde una conversación nueva y no tenga ese contexto. La v23 en concreto junta el trabajo de dos conversaciones distintas hechas en paralelo sobre la v18 — una con la v19→v22 de aquí abajo, otra con lo que se resume en la entrada v22→v23 — combinado a mano en una sola entrega. A partir de la v23 el proyecto volvió a bifurcarse en dos conversaciones distintas trabajando en paralelo, cada una con su propia numeración a partir de ahí — por eso hay dos entradas tituladas **v23 → v24**, una por cada rama; no es un error. La v29 es la fusión a mano de las dos: coge la v28 (que es donde llegó la rama que siguió numerando v25, v26, v27, v28) y le aplica encima, adaptados a lo que había cambiado mientras tanto, los cuatro cambios de la otra rama (la que se quedó en su propia v24, resumida en la primera de las dos entradas de abajo). Ver la entrada v28 → v29, al final de este historial, para el detalle de cómo se hizo esa fusión.

**v18 → v19 — Scroll: solo las tareas se mueven, el resto queda fijo.**
Problema: en proyectos/listas largas, la topbar, la barra de filtros, la barra flotante de selección múltiple y el pie de la barra lateral (Nuevo proyecto, tu usuario, Cerrar sesión) se desplazaban con el resto de la página en vez de quedarse fijos. Causa raíz: una sola línea de CSS — `.app-shell` usaba `min-height: 100vh` en vez de `height: 100vh`. Al no tener una altura estricta, en cuanto el contenido de dentro superaba el alto de la pantalla, era el propio `app-shell` el que crecía y acababa siendo el `body` quien hacía scroll. El resto del CSS ya estaba preparado para el comportamiento correcto (`.main-content` y `.sidebar__list` con `overflow-y:auto`, la barra de selección anclada con `position:absolute` dentro de `.main-col`) — solo le faltaba ese contenedor con altura acotada al viewport para activarse. Cambio: `.app-shell { height: 100vh; height: 100dvh; overflow: hidden; }` más `min-height: 0` en la cadena de contenedores flex intermedios (`.main-col`, `.sidebar`, `.sidebar__list`, `.main-content`) para que puedan encogerse y activar su propio scroll interno. Solo `css/styles.css` — sin cambios de JS ni de HTML.

**v19 → v20 — Nueva acción masiva: "Mover a mis tareas".**
En el menú "⋯ Más acciones" de la barra de selección múltiple (vista Lista): saca las tareas seleccionadas de su proyecto y las convierte en tareas personales de quien pulsa el botón — no de quien las tuviera asignadas antes. Sustituye los responsables anteriores por esa persona (si ya lo era, no cambia nada; si no, queda asignada automáticamente), porque al ser ya privada nadie más podría verla de todos modos. Pide confirmación antes, porque a diferencia del resto de acciones masivas, esta cambia quién puede ver la tarea, no solo un campo suyo. Cambios: `js/data/tasks.js` (nueva `bulkMoveToMyTasks(taskIds, uid)`), `js/components/bulk-toolbar.js` (la entrada de menú, su confirmación, y `currentUser` propagado hasta el menú "más acciones", que antes no lo recibía), `js/views/list-view.js` (le pasa `currentUser` a la barra). No hizo falta tocar `firestore.rules`: la regla de actualización de una tarea se evalúa sobre su estado *antes* del cambio, y una tarea de proyecto (`projectId != null`) ya está abierta a todo el equipo para escritura.

**v20 → v21 — Gestión de secciones, cabecera de tabla fija, y arreglo de las etiquetas de proyecto.** Tres cambios independientes:
- *Crear, renombrar y eliminar secciones.* Nuevo botón "🗂 Secciones" en la barra de herramientas de Lista, que abre un modal (`js/components/sections-modal.js`, mismo patrón que "Campos personalizados") para editar la lista completa antes de un único "Guardar". Al eliminar una sección con tareas dentro, esas tareas **no se borran**: `saveProjectSections()` (nueva, en `js/data/projects.js`) primero las pasa a `sectionId: null` por lotes, y solo después guarda la nueva lista de secciones. Se añadió un grupo/columna "Sin sección" tanto en Lista como en Tablero para que esas tareas sigan siendo visibles y gestionables, y el desplegable de sección dentro del propio detalle de tarea ganó la opción "— Sin sección —" (antes no existía forma de volver a poner una tarea sin sección desde ahí).
- *Cabecera de columnas fija en Lista y Mis tareas.* Los nombres de columna (con sus botones de ordenar y redimensionar) y la barra de herramientas se quedan arriba; solo las filas y las secciones se desplazan debajo. La pieza sutil: `.list-table-scroll` tiene `overflow-x:auto` para el scroll horizontal, y por especificación de CSS eso obliga al navegador a convertir también su `overflow-y` en `auto` aunque no se pida — en vez de pelear contra eso, se aprovechó dándole una altura acotada (con `.main-content:has(> .list-table-scroll) { overflow: hidden; }`, selector que solo afecta a estas dos vistas) y anclando `.list-table__header` con `position: sticky; top: 0` contra ese contenedor. Solo CSS, sin cambios de JS.
- *Nombres de proyecto largos ya no se salen de su etiqueta de color.* Dentro de una celda de tabla (que es un grid), un `<span>` se "blockifica" automáticamente y se estira al ancho exacto de su columna — con nombres largos, el texto se salía de ese ancho sin fondo detrás. Se arregló con `width: max-content; justify-self: start;` en `.tag-pill` (sin efecto fuera de una grid, así que no afecta a los demás sitios donde se usa esa clase): ahora el fondo coloreado siempre abraza el nombre completo, aunque con nombres muy largos eso implique que se extienda un poco sobre la columna siguiente.

**v21 → v22 — Checkbox en Mis tareas, gestión de archivados por clic derecho, y animación al completar.** Tres cambios independientes:
- *Círculo de completar en Mis tareas.* Ya existía en Lista pero faltaba en `js/views/my-tasks-view.js` — mismo comportamiento: marca/desmarca sin abrir el detalle de la tarea.
- *Desarchivar / eliminar proyectos por clic derecho.* El botón suelto "Desarchivar" de la vista Archivo se sustituyó por un menú contextual (clic derecho sobre cada fila), igual que el de proyectos activos en la barra lateral: "Desarchivar proyecto" y "Eliminar proyecto" (con confirmación; borra también todas sus tareas y comentarios). `js/app.js` le pasa a `renderArchiveView` un `onDelete` nuevo que reutiliza `deleteProjectWithTasks`, que ya existía para proyectos activos.
- *Animación de recompensa al completar tareas.* Nuevo módulo `js/components/celebration.js`, solo CSS + DOM (sin canvas ni librerías) y que respeta `prefers-reduced-motion` (si el sistema lo pide, no se dispara nada — la tarea se completa igual). Dos niveles: al marcar **una** tarea (círculo individual, en Lista o Mis tareas) sale un chispazo discreto de confeti desde el propio círculo, que además hace un pequeño "pop"; al marcar **varias de golpe** ("Marcar como completadas" en el menú "⋯" de la selección múltiple) cae confeti por todo el ancho de la pantalla (más piezas cuantas más tareas) más un mensaje tipo "🎉 5 tareas completadas — Imparable.". Los colores del confeti son los mismos que ya usa la app para etiquetas (`TAG_COLOR_PALETTE`, en `js/data/tags.js`), para no desentonar con el resto del diseño.

**v22 → v23 — Editor de descripción, filtro de responsables ficticios, "Bloqueada por" fuera, y filtros de Mis tareas recordados.** Cuatro cambios independientes, hechos en una conversación en paralelo a la v19→v22 de arriba:
- *Editor de descripción enriquecido y campo más grande.* El campo Descripción del modal de tarea pasa de un `<textarea>` de texto plano a un editor de texto enriquecido (nuevo `js/components/rich-text-editor.js`, reutilizable): barra de herramientas con negrita/cursiva/subrayado/tachado/listas/cita/enlace/código, y menú "/" para insertar bloques (párrafo, títulos H1-H3, listas, cita, bloque de código) — mismo patrón que el editor de Asana. El área editable pasa de 72px de alto mínimo a 220px. La descripción se guarda como HTML en vez de texto plano; `sanitizeHtml()` y `toEditableHtml()` (nuevas, en `js/utils.js`) limpian ese HTML antes de guardarlo en Firestore (nada de `<script>`, atributos `on...` ni enlaces `javascript:` — cualquiera del equipo puede editar la descripción de cualquier tarea de proyecto) y convierten a HTML seguro las descripciones antiguas en texto plano (incluidas las importadas de Asana), que se siguen viendo con normalidad hasta que se editen y guarden de nuevo. `mergeTasks()` en `js/data/tasks.js` también se actualizó para concatenar descripciones como HTML al combinar tareas duplicadas.
- *Solo cuentas reales al asignar responsables.* Los selectores de responsables (modal de tarea, "asignar a una persona" y "agregar colaboradores" de la barra de selección múltiple) ya no ofrecen las cuentas ficticias del importador de Asana. Excepción: si una tarea concreta ya tenía asignada una persona ficticia sin equivalencia aplicada todavía, esa se sigue viendo (marcada "· Asana") para no perderla de vista.
- *Fuera el campo "Bloqueada por".* Ocupaba mucho espacio en el modal de tarea y apenas se usaba. El dato (`dependsOn`) sigue en Firestore para tareas que ya lo tuvieran (y `mergeTasks()` lo sigue uniendo entre tareas fusionadas), pero ya no hay forma de verlo ni editarlo desde la interfaz.
- *Filtros de "Mis tareas" recordados por persona.* Se guardan en `localStorage` (namespaced por `uid`, mismo criterio que `sidebarCollapsed`) y se recuperan al volver a esa vista. La primera vez, sin nada guardado, se aplica "Pendiente" de serie para no enterrar la vista bajo lo ya completado; el contador de arriba pasa a decir "X de Y tareas" cuando el filtro oculta algo. Cambios: `js/app.js` (`loadMyTasksFilters()`/`saveMyTasksFilters()`, y `selectMyTasks()`/`handleFilterChange()` las usan).

A partir de aquí el proyecto se bifurca en dos conversaciones distintas trabajando en paralelo sobre esta misma v23, cada una con su propia numeración — de ahí las dos entradas **v23 → v24** seguidas (una por rama). La v29, al final, es la fusión de las dos.

**v23 → v24 (rama 1 de 2) — Recordar la última sección visitada, reordenar columnas arrastrando, orden por defecto por fecha+prioridad y columna de Etiquetas.** Cuatro mejoras de calidad de vida pedidas juntas en la misma conversación:
- *Recordar la última sección visitada al entrar.* Antes, si no había ya un proyecto seleccionado, `bootstrap()` (`js/app.js`) auto-seleccionaba siempre el primer proyecto de la lista (`projects[0].id`) sin más criterio que el orden en que Firestore los devolviera. Ahora se recuerda la última sección visitada por esa persona — un proyecto concreto (con la vista que tenía abierta: Lista/Tablero/Calendario/Línea de tiempo), Mis tareas, la Línea de tiempo global o el Archivo — y se aterriza ahí directamente. Se guarda en `localStorage` namespaced por `uid` (`chusy:lastLocation:<uid>`), mismo criterio que `sidebarCollapsed` y los filtros de Mis tareas: preferencia de este navegador, no de la cuenta (apartado 7). Sin nada guardado (primera entrada en este navegador) o si el proyecto recordado ya no existe (se borró o se archivó), el destino por defecto es "Mis tareas", tal y como se pidió. Cambios en `js/app.js`: nuevas `loadLastLocation()` / `saveLastLocation()` / `restoreLastLocation()`, llamada esta última una sola vez por sesión desde `bootstrap()` (bandera `hasRestoredLocation`, reseteada en `cleanup()` para que si otra persona inicia sesión en el mismo navegador recupere SU última sección, no la de quien salió); `selectProject()` / `selectMyTasks()` / `selectTimeline()` / `selectArchive()` guardan la ubicación al entrar en cada una, y el `onViewChange` de la topbar guarda también qué vista se eligió dentro de un proyecto. De paso se simplificó el callback de `subscribeToAllProjects()`: ya no hace falta el caso "sin `currentProjectId`, seleccionar el primero", porque `restoreLastLocation()` siempre deja `mode` en un estado concreto antes de que lleguen los primeros datos de Firestore.
- *Reordenar columnas arrastrando su cabecera.* Nuevo en `js/components/table-columns.js`: arrastrar el título de una columna y soltarlo sobre otra la mueve a esa posición, antes o después según de qué lado del centro se suelte — mismo patrón de HTML5 drag & drop que ya usaban las tarjetas del Tablero (`board-view.js`). "Nombre" sigue sin poder ocultarse y ahora tampoco se puede arrastrar (no lleva `draggable`): queda siempre como primera columna, de ancla. A diferencia del ancho y de qué columnas están ocultas (`columnPrefs`, que siguen siendo *por ámbito* — un proyecto concreto o "mytasks" — cada uno con lo suyo), el **orden es único y global por persona**, tal y como se pidió: cambiarlo en Mis tareas o en cualquier proyecto lo cambia en todos los sitios donde esas columnas existan. Vive en un campo nuevo del perfil, `users/{uid}.columnOrder` (suelto, fuera de `columnPrefs` — ver apartado 5), como un array de claves de columna en el orden elegido; una clave ausente ahí (una columna que nunca se ha movido, o exclusiva de un contexto — un campo personalizado de un proyecto en concreto, o "Proyecto" que solo existe en Mis tareas) se coloca al final, en su orden de siempre. Funciones nuevas: `setColumnOrder(uid, order)` en `js/data/users.js`; `applyColumnOrder(columns, order)`, `moveColumnKey(allColumns, order, draggedKey, targetKey, before)` (calcula el nuevo array tras un arrastre — separada de `wireColumnReorder` a propósito, para poder probarla sin DOM) y `wireColumnReorder(container, {...})` en `js/components/table-columns.js`. `resolveColumns()` gana un tercer parámetro `order`, y `openColumnsMenu()` ahora también recibe `order` para listar el popover de mostrar/ocultar en el mismo orden en que aparecen las columnas en la tabla. `list-view.js` y `my-tasks-view.js` pasan `currentUser.columnOrder` en cada llamada y conectan `wireColumnReorder()` junto al `wireColumnResize()` que ya tenían.
- *Orden y agrupación de "Mis tareas" por fecha.* Dos ajustes relacionados. Primero, sin pulsar ninguna columna, `sortTasks()` (`js/task-filters.js`) ordenaba solo por prioridad — con varias tareas de la misma prioridad (el caso habitual), el orden real dependía de cómo las devolviera Firestore, sin ningún sentido para quien mira la lista. Ahora el orden por defecto (`sortState.column === null`, el estado inicial al entrar en una sección o tras cambiar de filtro) es fecha límite ascendente — sin fecha siempre al final — y, dentro de la misma fecha (o entre las que no tienen), por prioridad; una columna explícita se sigue comportando igual que antes (empate exacto = orden de llegada, sin desempate añadido). Segundo, la agrupación de "Mis tareas" en bloques por urgencia (`js/views/my-tasks-view.js`) ya existía desde antes de esta versión pero nunca se había documentado aquí — aprovechando que se pidió de nuevo (inspirándose en una captura de la propia "Mis tareas" de Asana), se afinó para acercarla más a esa referencia: nuevo bloque **"Mañana"** (antes cualquiera de esas tareas caía dentro de "Esta semana", ahora renombrado a **"Próximos 7 días"** porque describe con más precisión lo que hace: una ventana móvil de 7 días desde hoy, no "lo que queda de la semana natural"). Bloques resultantes y sus límites: Vencidas (pasado y pendiente — si además está completada cae en Hoy, comportamiento ya existente sin tocar), Hoy, Mañana, Próximos 7 días (de pasado-mañana a 6 días vista), Más adelante (desde el día 7, límite exclusivo) y Sin fecha. Como este componente agrupa sobre la lista que ya le llega preordenada desde `sortTasks()`, el cambio del orden por defecto también ordena correctamente las tareas *dentro* de cada bloque sin tocar nada más.
- *Etiquetas: columna propia, ordenable.* Antes las etiquetas de una tarea (hasta 2, el resto se recortaban) aparecían pegadas al título en la celda de "Nombre", sin columna propia — no había forma de ordenar por ellas ni de ver todas si eran más de dos (lo que quería un compañero que trabaja mucho con etiquetas). Ahora hay una columna "Etiquetas" (`key: "tags"`) en `list-view.js` y `my-tasks-view.js`, con las mismas capacidades que el resto: ordenar, redimensionar, ocultar/mostrar y reordenar por arrastre. Al tener ya su propio sitio, se quitaron de la celda "Nombre" — si se oculta la columna "Etiquetas", las etiquetas de una tarea dejan de verse en la tabla (siguen intactas y editables desde el detalle de la tarea). Una tarea puede llevar varias etiquetas a la vez, así que `sortTasks()` ordena por la que sea alfabéticamente primera entre las suyas (mismo criterio que ya usaba "Responsables", que ordena por el primero de su lista). Nueva clase CSS `.list-table__tags-cell` (flex con `wrap`, para que varias etiquetas se acomoden en la celda en vez de desbordar).

Los cuatro cambios se probaron aparte con Node antes de entregar (no forma parte del repositorio): un script importa directamente `applyColumnOrder()`/`moveColumnKey()` de `table-columns.js` y `sortTasks()` de `task-filters.js` con datos de ejemplo, y otro replica la agrupación por fecha de `my-tasks-view.js` con una fecha de referencia fija — para confirmar empates, columnas ocultas/desconocidas y los límites exactos de cada bloque de fecha sin depender de abrir la app a mano.

**v23 → v24 (rama 2 de 2) — Modo claro/oscuro, recompensa por rachas fuera de la selección múltiple, selección múltiple en Mis tareas, y arreglo del icono descentrado en la barra lateral colapsada.** Cuatro cambios independientes:
- *Modo claro/oscuro, elegible desde "Mi cuenta".* Toda la interfaz ya usaba variables CSS para el color (bloque `:root` en `css/styles.css`), así que el modo claro es un segundo bloque de esas mismas variables bajo `:root[data-theme="light"]` — sin tocar reglas una a una. Ese atributo `data-theme` en `<html>` es lo único que distingue un tema del otro; lo pone `js/theme.js` (`applyTheme()` / `getCachedTheme()`), cacheándolo en `localStorage` bajo la clave `chusy:theme`. Para que no se vea ni un parpadeo del tema equivocado mientras carga Firebase, un script embebido al principio de `<head>` en `index.html` lee esa misma clave de forma síncrona y aplica el atributo antes del primer pintado (la pantalla de carga que ya existía cubre ese hueco de sobra). La cuenta (`users/{uid}.theme`, vía `updateUserProfile`) es la fuente de verdad de verdad, para que el tema viaje entre dispositivos: `js/app.js` la aplica en cuanto se conoce el perfil (dentro de `onAuthChange`), reconciliándola con la caché local si difieren. El selector en sí vive en `js/components/account-modal.js` — nueva sección "Apariencia" con dos `.chip` (reutiliza el estilo de las píldoras de prioridad del modal de tarea, cero CSS nuevo) que aplican al momento y guardan en Firestore en segundo plano; si ese guardado falla, el tema se queda igualmente aplicado en este navegador y solo se avisa de que no viajará a otro dispositivo. Detalle no trivial: varios sitios usaban `--color-void` (el fondo de página, muy oscuro en el único tema que existía hasta ahora) como color de *texto* para ponerlo encima de `--color-signal` (el dorado) u otros fondos brillantes — insignia de admin, botón primario, "hoy" del calendario, contador de filtros, texto de las barras del calendario. Al volverse `--color-void` un color claro en el tema claro, ese texto habría dejado de leerse; se creó `--color-ink` (texto oscuro fijo, igual en los dos temas, pensado justo para ir encima de fondos brillantes) y se corrigieron esos puntos (cinco en `css/styles.css`, uno más en `account-modal.js` que además tenía los colores en hexadecimal suelto en vez de variables — la insignia de rol). También se ajustó `--color-signal` específicamente para el tema claro, de `#FCD000` a `#8A6D00`: el dorado original da muy poco contraste como texto sobre blanco (~1,5:1 — prácticamente ilegible), y ese mismo token se usa como color de enlaces y acentos por toda la interfaz, no solo como fondo de insignias. El resto de la paleta (rojo/ámbar/verde de estado, el dorado atenuado de bordes, los "washes" translúcidos) se dejó igual en ambos temas a propósito: son washes de baja opacidad que ya se adaptan solos al fondo que tengan detrás, o colores de estado que conviene reconocer igual en cualquier tema.
- *Modo claro/oscuro, elegible desde "Mi cuenta".* Toda la interfaz ya usaba variables CSS para el color (bloque `:root` en `css/styles.css`), así que el modo claro es un segundo bloque de esas mismas variables bajo `:root[data-theme="light"]` — sin tocar reglas una a una. Ese atributo `data-theme` en `<html>` es lo único que distingue un tema del otro; lo pone `js/theme.js` (`applyTheme()` / `getCachedTheme()`), cacheándolo en `localStorage` bajo la clave `chusy:theme`. Para que no se vea ni un parpadeo del tema equivocado mientras carga Firebase, un script embebido al principio de `<head>` en `index.html` lee esa misma clave de forma síncrona y aplica el atributo antes del primer pintado (la pantalla de carga que ya existía cubre ese hueco de sobra). La cuenta (`users/{uid}.theme`, vía `updateUserProfile`) es la fuente de verdad de verdad, para que el tema viaje entre dispositivos: `js/app.js` la aplica en cuanto se conoce el perfil (dentro de `onAuthChange`), reconciliándola con la caché local si difieren. El selector en sí vive en `js/components/account-modal.js` — nueva sección "Apariencia" con dos `.chip` (reutiliza el estilo de las píldoras de prioridad del modal de tarea, cero CSS nuevo) que aplican al momento y guardan en Firestore en segundo plano; si ese guardado falla, el tema se queda igualmente aplicado en este navegador y solo se avisa de que no viajará a otro dispositivo. Detalle no trivial: varios sitios usaban `--color-void` (el fondo de página, muy oscuro en el único tema que existía hasta ahora) como color de *texto* para ponerlo encima de `--color-signal` (el dorado) u otros fondos brillantes — insignia de admin, botón primario, "hoy" del calendario, contador de filtros, texto de las barras del calendario. Al volverse `--color-void` un color claro en el tema claro, ese texto habría dejado de leerse; se creó `--color-ink` (texto oscuro fijo, igual en los dos temas, pensado justo para ir encima de fondos brillantes) y se corrigieron esos puntos (cinco en `css/styles.css`, uno más en `account-modal.js` que además tenía los colores en hexadecimal suelto en vez de variables — la insignia de rol). También se ajustó `--color-signal` específicamente para el tema claro, de `#FCD000` a `#8A6D00`: el dorado original da muy poco contraste como texto sobre blanco (~1,5:1 — prácticamente ilegible), y ese mismo token se usa como color de enlaces y acentos por toda la interfaz, no solo como fondo de insignias. El resto de la paleta (rojo/ámbar/verde de estado, el dorado atenuado de bordes, los "washes" translúcidos) se dejó igual en ambos temas a propósito: son washes de baja opacidad que ya se adaptan solos al fondo que tengan detrás, o colores de estado que conviene reconocer igual en cualquier tema.
- *Recompensa grande también fuera de la selección múltiple: rachas rápidas y del día.* `js/components/celebration.js` reescrito para trackear dos rachas nuevas, además de la ya existente por selección múltiple (ahora identificada internamente como `kind: "selection"`). Racha rápida (`kind: "streak"`): cada `celebrateTask()` (se llama al marcar una tarea individual, en Lista o Mis tareas) guarda su marca de tiempo en una cola en memoria; con 3 o más dentro de los últimos 9 segundos salta la recompensa grande con ese recuento, y sigue saltando en cada una más mientras la racha no se enfríe, como un contador de combo. Racha del día (`kind: "daily"`): un contador en `localStorage` (clave `chusy:dailyCompletions`, guarda también la fecha — se reinicia solo al cambiar de día) que suma con cada completada, incluidas las hechas por selección múltiple (para que el total del día sea real sin importar cómo se complete cada una); al cruzar uno de los hitos (5, 10, 15, 20, 30, 50, 75, 100) salta la recompensa grande mostrando el total acumulado de hoy, no el hito exacto — si una selección múltiple salta de 3 a 8 de golpe, por ejemplo, se cruza el hito de 5 pero el mensaje dice "8 tareas hoy". Si coinciden las dos rachas a la vez, gana la del día (para no lanzar dos recompensas de golpe). Cada tipo tiene su propio emoji y sus propias frases, agrupadas en `CELEBRATION_KINDS`. `celebrateBulk(count, kind)` sigue aceptando tal cual las llamadas ya existentes (`kind` por defecto es `"selection"`), así que `bulk-toolbar.js` no necesitó tocarse para esto — sí gana, por dentro de ese mismo `kind`, la contribución al contador del día.
- *Selección múltiple también en Mis tareas.* `js/views/my-tasks-view.js` gana su propio `createSelectionController()` (de `bulk-selection.js`, que ya era genérico y no necesitó ningún cambio) con el mismo comportamiento que Lista: Ctrl/Cmd+clic para añadir/quitar una tarea, Shift+clic para el tramo, y la misma barra flotante de `bulk-toolbar.js`. Como aquí la selección puede mezclar tareas de varios proyectos a la vez y recordatorios personales (a diferencia de Lista, donde todas son siempre del mismo proyecto), `renderBulkToolbar()` pasa a recibir `project` como parámetro opcional: sin él, "Mover a otra sección" se oculta del todo (las secciones son de un proyecto concreto, no hay uno de referencia posible) y "Cambiar de proyecto" deja de excluir "el proyecto actual" de la lista de destinos (no hay uno) y ofrece todos. El resto de acciones (asignar, fechas, eliminar, marcar completadas, agregar colaboradores, combinar duplicadas, convertir en hitos, mover a mis tareas) no dependían de un proyecto único y siguen funcionando sin tocarlas.
- *Icono de "Mis tareas" descentrado en la barra lateral colapsada.* El contador de pendientes junto a "Mis tareas" (`.sidebar__item-count`) es también un `.sidebar__label`, así que al colapsar la barra ya le tocaba `max-width:0; opacity:0` igual que al texto del nombre — pero al tener además `padding` y `border` propios, esa caja no llegaba a pesar 0px de verdad (ninguno de los dos encoge por debajo de su propio tamaño aunque el contenido sí lo haga a 0). Ese resto invisible-pero-presente descuadraba el `justify-content:center` del icono frente al resto de items de la barra (que, al no tener ese tercer elemento oculto, ya quedaban perfectamente centrados). Arreglo: `display:none` en el contador cuando la barra está colapsada, con su contrapartida `display:inline-block` dentro del `@media (max-width:860px)` — ahí la barra colapsada se ve siempre "expandida" por pantalla, así que el contador sí debe verse. Solo `css/styles.css`.

Las siguientes cuatro entradas, hasta la v28, siguen siendo la misma rama 2 continuando (numeración propia de esa conversación, sin relación con la rama 1 de arriba):

**v24 → v25 — El scroll de Lista y Mis tareas se iba arriba del todo al completar una tarea o tocar la selección múltiple.**
Problema: en listas largas, completar una tarea (el checkbox de la fila, o "Marcar como completada" del menú contextual) o simplemente añadir/quitar una tarea de la selección múltiple (Ctrl/Cmd+clic, Shift+clic) mandaba el scroll arriba del todo, obligando a bajar de nuevo para seguir donde estabas. Causa raíz: el contenedor que de verdad hace scroll en estas dos vistas no es `.main-content` (el que se pasa como `container` a cada vista) sino `.list-table-scroll`, un div de dentro — y ESE sí forma parte del HTML que `renderListView()`/`renderMyTasksView()` reconstruyen entero en cada render, con `container.innerHTML = ...`. `.main-content` en sí nunca se recrea (solo se le reasigna el contenido de dentro), así que su `scrollTop` sobrevive solo — por eso Tablero, Calendario, Línea de tiempo y Archivo (que scrollean directamente sobre `.main-content`) nunca han tenido este problema. Pero un nodo `.list-table-scroll` nuevo no hereda el `scrollTop` del que sustituye, aunque ocupe su mismo sitio en la página: por eso se iba a 0 cada vez, y "cada vez" incluye cualquier actualización en tiempo real de Firestore, no solo las causadas por quien mira la pantalla en ese momento. Es un efecto colateral de cómo se resolvió la cabecera fija en la v20→v21 (ver esa entrada más arriba): mover el scroll a ese div interno fue lo que hizo posible la cabecera pegajosa, pero se quedó pendiente conservar su posición al reconstruirlo — probablemente ha sido así desde entonces, solo que pasaba menos desapercibido antes de que la selección múltiple (v23→v24, y antes solo en Lista) multiplicara cuántas veces por sesión se reconstruye la tabla. Arreglo: nueva `setListHtml(container, html)` en `js/utils.js` — lee `scrollTop`/`scrollLeft` del `.list-table-scroll` de ANTES de tocar nada, reasigna el `innerHTML` como siempre, y los vuelve a aplicar sobre el `.list-table-scroll` nuevo; todo síncrono, sin parpadeo perceptible. `list-view.js` y `my-tasks-view.js` la usan en el único punto de cada uno donde reconstruyen la tabla (en Mis tareas, no en la rama de "sin tareas asignadas", que no tiene `.list-table-scroll` que conservar). El resto de vistas no la necesitan.

**v25 → v26 — Modo claro: el dorado oscurecido no leía bien y no convencía, y el gris de fondo se veía azulado.** Dos ajustes de la paleta clara, a partir de feedback directo tras probarla, sin tocar nada de JS:
- *El dorado vuelve a ser el mismo vivo de siempre, salvo en un puñado de sitios de texto suelto.* La v23→v24 había oscurecido `--color-signal` entero para el tema claro (`#FCD000` → `#8A6D00`), porque el dorado original apenas se distingue como texto sobre blanco. Pero esa misma variable también se usa como FONDO con `--color-ink` (texto oscuro fijo) encima — botón primario, insignia de admin, "hoy" del calendario, contador de filtros — y ahí oscurecer el dorado fue en la dirección equivocada: acerca el fondo al tono del propio texto oscuro, bajando el contraste en vez de subirlo (con el dorado vivo de siempre daba ~13:1; con el oscurecido, ~4:1 — se nota, y en emojis/números pequeños en negrita se nota todavía más). Solución: `--color-signal` deja de redefinirse para el tema claro — vale `#FCD000` en los dos temas, así que todo lo que sea fondo, relleno decorativo (hitos, puntos, la rayita de acento de "hoy"...) o un simple cambio de color en :hover recupera el dorado de siempre. Se creó `--color-signal-text` (por defecto igual a `--color-signal`, así que en modo oscuro no cambia nada) para los pocos sitios donde el dorado hace de color de texto SUELTO y sostenido sobre una superficie clara — no de fondo, no hover-only —, y solo ahí, en el tema claro, vale un tono más oscuro (`#96700A`, ~4,5:1 sobre blanco): el enlace genérico (`a`), el contorno de foco de accesibilidad (`:focus-visible`), los enlaces y el código en línea dentro de la descripción de una tarea, y unas pocas etiquetas que se quedan encendidas mientras dura ese estado — no solo al pasar el ratón — como la columna por la que se está ordenando, la pestaña de vista activa, un filtro activo, o un chip de prioridad seleccionado. Trece sitios en total pasaron a `--color-signal-text`; el resto (seguían siendo mayoría) se quedó tal cual con `--color-signal`.
- *Gris de fondo del cuerpo de la página, menos azulado y un pelín más claro.* `--color-void` (el `body`) y `--color-void-raised` (inputs, bloques de código — su vecino más cercano en la escala tonal) tenían un ligero sesgo azul (`#F1F2F4`/`#F8F9FA`, con el canal B por encima del R) heredado de intentar mantener el mismo tinte "slate" de marca que el resto de la paleta clara. Pasan a `#F4F4F4`/`#F9F9F9` — gris neutro de verdad (R=G=B) y un poco más claro que antes. El resto de la paleta clara (`--color-panel-raised`, líneas, texto) no se tocó: no era lo que se pedía, y ya son grises bastante neutros de por sí.

---

**v26 → v27 — El gris de fondo seguía sin convencer: demasiado presente.** `--color-void` pasa de `#F4F4F4` a `#FCFCFB` (con `--color-void-raised` reescalado en proporción, a `#FDFDFC`) — un fondo casi blanco, apenas un pelín por debajo del blanco puro de `--color-panel`, en vez del gris claro de antes. Sigue siendo neutro (sin el sesgo azul de la v25→v26 hacia atrás), simplemente mucho más sutil. Funciona sin sobresaltos porque los sitios donde `--color-panel` (blanco) se apoya sobre `--color-void` para distinguirse — la barra lateral, la tarjeta de inicio de sesión — ya llevaban su propio `border` de separación (no dependían solo del contraste de color), así que perder casi todo el contraste entre los dos grises no los deja "flotando" sin borde. Efecto secundario a vigilar: en el Calendario, los días de fuera del mes (`.cal-daybg.is-other-month`) se distinguen de los del mes en curso SOLO por este mismo contraste de fondo (`--color-void` vs `--color-void-raised`, sin ningún otro cambio de opacidad o color en el número del día) — con los dos tan pegados al blanco, esa distinción se nota ahora mucho menos que antes. No se ha tocado nada para compensarlo (no se pidió, y el borde entre celdas se mantiene igual); si se nota demasiado sutil de aquí, se puede dar a `.is-other-month` un tratamiento propio en vez de depender de estos dos tokens.

---

**v27 → v28 — Días de fuera de mes más oscuros en Calendario (con Línea de tiempo arreglada de paso), y el topbar no se actualizaba al cambiar de vista.** Dos cambios independientes:
- *Nueva variable `--color-dim`, para "esta celda es secundaria dentro de una cuadrícula", desligada de `--color-void`/`--color-void-raised`.* Como se avisaba en la entrada anterior, los días de fuera de mes en Calendario (`.cal-daybg.is-other-month`) habían quedado casi indistinguibles del resto al acercar tanto `--color-void` a blanco — y resulta que Línea de tiempo tenía exactamente el mismo problema sin que nadie lo hubiera pedido todavía: los fines de semana (`.tl-daycol.is-weekend`) usaban `--color-void-raised` con la misma idea de sombrearlos un poco, y por el mismo motivo también se habían quedado casi invisibles. En vez de aclarar todavía más esos dos tokens generales (que cumplen su papel en el resto de la interfaz) o mantenerlos atados a lo que le convenga al fondo general de la página, se creó `--color-dim` — una variable solo para esto, un gris claro-medio bien diferenciado (`#E7E8EA` en modo claro; en oscuro, `#1A1B1D`, a medio camino entre los dos valores que usaba antes, así que ahí no cambia nada a la vista). La usan ambos sitios (`.cal-daybg.is-other-month` y `.tl-daycol.is-weekend`); el resto de usos de `--color-void`/`-raised` (campos de texto, código, popovers…) no se tocó — son superficies con su propio borde, no dos estados que haya que distinguir entre sí, así que nunca tuvieron este problema.
- *El topbar (Lista/Tablero/Calendario/Línea de tiempo) no actualizaba la pestaña resaltada al cambiar de vista.* El contenido sí cambiaba, pero el botón marcado como activo se quedaba en la vista anterior. Causa: `renderTopbar()` solo se invocaba desde dentro de `renderShell()`, mientras que el propio clic en una pestaña (`onViewChange`) solo llamaba a `renderMain()` — una función más barata, pensada para repintar `.main-content`/la barra de filtros sin tocar el resto del chrome — así que el topbar nunca se volvía a pintar con el `currentView` nuevo hasta el siguiente render completo (cambiar de proyecto, recargar…). El propio cambio de vista funcionaba bien (la web SÍ mostraba la vista correcta), era puramente la pestaña resaltada la que se quedaba desactualizada. Arreglo: la llamada a `renderTopbar()` se sacó a su propia función, `renderProjectTopbar()`, y `onViewChange` la llama a ella además de a `renderMain()` — mismo patrón que `syncBulkToolbar()` en Lista/Mis tareas (una función dedicada a refrescar SOLO la pieza que puede quedarse obsoleta, en vez de forzar un render completo de todo el shell por un cambio tan local). Solo `js/app.js`.

---

**v28 → v29 — Fusión de las dos ramas paralelas: la v24 de la rama 1 (recordar sección, columnas arrastrables, orden por fecha+etiquetas) aplicada sobre la v28 de la rama 2 (modo claro, selección múltiple en Mis tareas, confeti por rachas, los arreglos de paleta y de scroll).** No es un cambio de funcionalidad nuevo — es la combinación a mano de trabajo ya hecho en dos conversaciones distintas, siguiendo el mismo criterio que ya se usó para construir la v23 (ver la introducción de este historial). Se hizo con una comparación de tres vías: la v23 como ancestro común, la v24 de la rama 1 y la v28 de la rama 2, para saber con precisión qué había tocado cada rama por separado y no perder nada de ninguna.

De los archivos que toca la rama 1, tres no los había tocado la rama 2 para nada (`js/components/table-columns.js`, `js/data/users.js`, `js/task-filters.js`): se copiaron sin más. Los otros cuatro sí se cruzaban con trabajo de la rama 2, y se fusionaron a mano, punto por punto:
- *`js/app.js`.* El único choque real: la rama 2 había extraído a una función aparte, `renderProjectTopbar()`, el bloque que antes montaba el topbar directamente dentro de `renderShell()` — justo donde la rama 1 había añadido su `onViewChange` con el guardado de la última vista. Se aplicó el guardado (`saveLastLocation(...)`) dentro de esa función ya extraída, en vez de sobre la versión antigua. El resto de cambios de la rama 1 (`loadLastLocation`/`saveLastLocation`/`restoreLastLocation`, la bandera `hasRestoredLocation`, y que `selectProject()`/`selectMyTasks()`/`selectTimeline()`/`selectArchive()` guarden la ubicación) caían en zonas que la rama 2 no había tocado, así que se aplicaron tal cual.
- *`js/views/my-tasks-view.js`.* El archivo con más riesgo de los cuatro, porque la rama 2 le añadió selección múltiple completa (controlador, filas seleccionables, barra flotante) tocando gran parte de la función. Ningún cambio de la rama 1 caía literalmente encima de esas líneas — la columna "Etiquetas", el bloque "Mañana" y el resto se aplicaron sin más que adaptar la firma de la función (la rama 2 la había cambiado a recibir un único `opts` para poder reutilizarlo en `rerenderSelf()`), y usar `setListHtml()` (de la rama 2, para no perder el scroll) en vez del `container.innerHTML` directo que tenía la rama 1.
- *`js/views/list-view.js`.* Cambio mínimo de la rama 2 aquí (solo cambiar `container.innerHTML` por `setListHtml()`, mismo motivo que arriba): se aplicó sobre la versión de la rama 1, que es la que llevaba el grueso de cambios en este archivo (columna de etiquetas, `wireColumnReorder`).
- *`css/styles.css`.* Sin choques de verdad — la rama 2 tocó la paleta de colores (variables del modo claro/oscuro) en sitios que la rama 1 no había tocado. Al insertar las reglas nuevas de la rama 1 (indicadores de arrastre de columna, celda de etiquetas) se revisó que siguieran usando las variables correctas según la convención que dejó la rama 2 (`--color-signal` para fondos y rellenos decorativos, `--color-signal-text` para texto suelto) — el indicador de "aquí se suelta la columna" es un `box-shadow` decorativo, no texto, así que le corresponde `--color-signal` tal cual ya lo tenía.

`README.md` (este archivo) se fusionó también a mano: el apartado 3 lleva ya las descripciones de ambas ramas (actualizando la de columnas para mencionar el arrastre, y añadiendo las de Etiquetas, agrupación por fecha y última sección visitada); el modelo de datos junta `theme` y `columnOrder` en la misma línea de `users/{uid}`; y las dos notas sueltas sobre "esto es de este navegador, no de la cuenta" (una de cada rama) se consolidaron en un único par de puntos en el apartado 7, uno para lo que vive en Firestore y otro para lo que vive en `localStorage`, en vez de dejarlas repetidas por separado.

Antes de entregar se repitió la misma verificación con Node que ya se había hecho para la v24 de la rama 1 (`applyColumnOrder`/`moveColumnKey`/`sortTasks`/agrupación por fecha, contra el código ya fusionado, no contra el de antes de fusionar) y además dos comprobaciones nuevas pensadas específicamente para una fusión: sintaxis de los 39 archivos `.js` del proyecto uno por uno, y un script aparte que cruza cada `import { ... } from "..."` de todo el proyecto contra los `export` reales del archivo al que apunta, para pillar cualquier función que un archivo esperase de otro y que la fusión hubiera dejado sin existir. Las dos pasaron limpias.

---

**v29 → v30 — Cuatro mejoras independientes: cursor de redimensionar columna solo al arrastrar, buscador por texto en la barra de filtros, modo de apariencia "clásico", y tareas en varios proyectos a la vez (con responsables también en las personales).**

- *Cursor de redimensionar columna, solo durante el arrastre.* El borde derecho de cada columna (`.col-resize-handle`) tenía `cursor: col-resize` fijo, así que el ratón se volvía una mano con solo pasar por encima, sin necesidad de llegar a arrastrar — parecía indicar que ya se podía soltar la columna ahí mismo. La app ya distinguía el arrastre de verdad con una clase en el `body` (`is-col-resizing`, puesta en `wireColumnResize()` de `table-columns.js` al hacer `mousedown` y quitada al soltar) que fuerza ese mismo cursor con `!important` mientras dura — bastaba con quitar el `cursor: col-resize` del hover y dejar que esa regla, que ya existía, se encargue de todo. La rayita que se enciende al pasar el ratón cerca del borde (`:hover::after`) se deja igual, como aviso visual de que ahí hay algo que arrastrar, sin cambiar el cursor. Solo `css/styles.css`.

- *Buscador por texto en la barra de filtros.* Cuadro de texto al principio de la barra (`.filterbar__search`, junto a los chips de siempre) que filtra por título y por el texto plano de la descripción, sin mayúsculas ni acentos. `applyTaskFilters()` (`js/task-filters.js`) gana un tercer parámetro opcional, `searchText`, que se aplica como un paso más después de los filtros de chips existentes; dos helpers nuevos en `js/utils.js` lo hacen posible: `normalizeForSearch()` (minúsculas + `.normalize("NFD")` para quitar acentos) y `stripHtmlToText()` (texto plano a partir del HTML de la descripción, con un `<template>` desechable — mismo truco que ya usan otros sitios del navegador para esto, sin librerías). El filtro de "Proyecto" en la propia `applyTaskFilters()` también se generalizó de paso, para que una tarea en varios proyectos a la vez coincida si CUALQUIERA de los suyos está entre los marcados (antes solo miraba `t.projectId`) — ver el cambio de modelo de datos más abajo.

  La parte no trivial fue que el cuadro no perdiera el foco a media escritura. `renderFilterBar()` (`js/components/filter-bar.js`) reconstruye la barra entera con `innerHTML` en cada llamada — perfectamente válido para los filtros de chips (un clic no necesita conservar foco), pero un `<input>` de texto sí lo necesita: si cada pulsación de tecla disparara un re-render que vuelve a montar la barra completa, el `<input>` se destruiría y se recrearía en cada letra. Se resolvió por dos lados a la vez, no con un solo truco:
  1. `renderFilterBar()` llama a `onSearchChange` con un pequeño retardo tras la última pulsación (`debounce(fn, 200)`, que ya existía en `utils.js` sin usar hasta ahora), así que no se dispara un cambio de estado en cada tecla.
  2. En `js/app.js`, ese cambio (`handleSearchTextChange`) NUNCA vuelve a llamar a `renderFilterBar()` — solo repinta el contenido de debajo. `renderMain()` (modo proyecto) se dividió en dos: construir la barra de filtros por un lado, y una `renderProjectContent()` nueva con el resto (antes todo junto); mismo patrón nuevo para Mis tareas (`renderMyTasksContent()`, que también actualiza el contador del topbar — repintar el topbar entero no tiene el mismo problema, no lleva ningún campo de texto) y para la línea de tiempo global (`renderTimelineContent()`). `handleSearchTextChange()` llama solo a la función de contenido que toque según el modo; el resto de disparadores (clic en un chip, cambiar de vista, cambiar de proyecto…) se dejaron llamando a las funciones completas de siempre, sin tocar ese comportamiento.

  `searchText` es una variable de módulo más en `app.js` (como `activeFilters`), pero a propósito NO se guarda en ningún sitio entre sesiones — se reinicia en `selectProject()`/`selectMyTasks()`/`selectTimeline()` igual que `activeFilters`, salvo que `activeFilters` de Mis tareas sí se recuerda (apartado 7) y `searchText` nunca.

- *Modo de apariencia "clásico".* Tercera opción además de claro/oscuro, con los colores de Asana que se pidieron exactos: barra lateral `#2a2c2e`, cuerpo de página `#ffffff`, acento (botones, insignias, hitos…) `#ff584a` en vez del dorado de siempre. `js/theme.js` (`normalize()`) y el script embebido al principio de `index.html` (el que aplica el tema cacheado antes del primer pintado, para no dar un parpadeo) ahora reconocen `"classic"` además de `"light"`; el chip nuevo vive en `js/components/account-modal.js`, junto a los otros dos.

  Por dentro, en `css/styles.css`: nueva variable `--color-sidebar-bg` (por defecto `var(--color-panel)`, así que en oscuro y claro no cambia nada — una referencia `var()` se resuelve de forma perezosa contra lo que valga esa variable EN CADA punto del árbol, no se congela al valor que tuviera al declararse esta línea) de la que pasa a depender `.sidebar` en vez de `--color-panel` directamente, para poder aclarar el cuerpo de la página sin aclarar también la barra lateral. El bloque `:root[data-theme="classic"]` repite explícitamente los tokens de superficie/texto del modo claro (los atributos `[data-theme="X"]` no heredan nada entre sí, son selectores excluyentes, no una cascada) y solo cambia lo que distingue a "clásico": `--color-void` a `#ffffff` puro (el claro normal usa `#FCFCFB`, un "casi blanco" — aquí sí se pidió blanco exacto), `--color-sidebar-bg` a `#2a2c2e`, y `--color-signal` a `#ff584a`. `--color-signal-dim` y `--color-signal-text` usan un mismo tono coral oscurecido al 80% (`#CC463B`, ~4,7:1 de contraste sobre blanco, calculado con Python antes de escribirlo — el coral de base da solo ~3,1:1, poco para texto suelto) — mismo motivo por el que el modo claro ya oscurece el dorado para `--color-signal-text`, reutilizando aquí ese mismo cálculo también para `-dim` en vez de inventar un tercer valor que nadie pidió. `--color-signal-soft` es la misma fórmula de siempre (14% de opacidad) con el RGB del coral. `--color-ink` (el texto oscuro que va encima de los botones de color acento) no hizo falta tocarlo: contraste de sobra sobre el coral (~6,8:1).

- *Tareas en varios proyectos a la vez, y responsables en las tareas personales.* El cambio más grande de los cuatro, con dos partes pedidas juntas porque encajan: (1) desde el detalle de cualquier tarea, poder añadirla a más de un proyecto a la vez, cada uno con su propia sección — como en Asana, según la captura de referencia que sirvió de guía; y (2) que eso mismo, más asignar responsables, funcione también desde "Mis tareas" con tareas personales, para poder crear un recordatorio, asignarlo a otra persona y meterlo en un proyecto sin cambiar de ventana.

  *Modelo de datos.* `projectId`/`sectionId` se quedan exactamente con el significado de siempre (el proyecto y sección PRINCIPALES de la tarea — de ahí cuelga el permiso de lectura/escritura abierto a todo el equipo, sin tocar), y se añaden `extraProjectIds[]` (los proyectos ADICIONALES) y `extraSections{}` (mapa `{[projectId]: sectionId}`, porque cada proyecto tiene sus propias secciones — una tarea puede estar en una sección distinta dentro de cada uno). Se eligió deliberadamente NO sustituir `projectId` por un único array (`projectIds[]`) a pesar de ser más "limpio" sobre el papel: la regla de lectura de Firestore necesita poder demostrar, a partir de los propios filtros de cada consulta, que esa consulta es segura (ver la entrada v25→v26 del historial de "Mis tareas", y el comentario de `subscribeToMyTasks()` en `tasks.js`) — un array vacío o no vacío (`projectIds.size() > 0`) no es algo que Firestore pueda demostrar a partir de una consulta `array-contains`, mientras que un simple `projectId != null` sí, exactamente igual que ya funcionaba antes de este cambio. Mantener `projectId` como está y tratar `extraProjectIds` como un extra siempre acompañado de un `projectId` ya puesto (nunca al revés — se aplica al guardar, ver `computeOwnerIdOnSave()` en `task-modal.js`) evita tener que demostrarle nada nuevo a Firestore sobre el caso general, y reutiliza tal cual las reglas que ya estaban en producción.

  Dos helpers nuevos en `js/utils.js`, usados por todas las vistas en vez de leer estos campos a mano: `getTaskProjectIds(task)` (principal + adicionales, sin duplicados) y `getTaskSectionForProject(task, projectId)` (su sección dentro de ESE proyecto en concreto, sea principal o adicional).

  *`firestore.rules`.* La regla de lectura y de actualización de `tasks` gana una rama nueva: `request.auth.uid in resource.data.assigneeIds` — cualquiera que conste como responsable de una tarea puede leerla y editarla, sea de proyecto o personal (antes, una tarea personal solo la podía leer/editar su `ownerId`). No se tocó la regla de borrado: ser responsable de una tarea personal no basta para borrarla, sigue reservado a quien la creó o a un admin. Esta rama nueva simplificó de paso `subscribeToMyTasks()` (`tasks.js`): antes hacían falta dos consultas donde una acotaba `assigneeIds` array-contains CON `projectId != null` a la vez, porque la regla de entonces no tenía ninguna rama que cubriera "eres responsable de una tarea personal" y había que demostrar la seguridad de la consulta por el lado de "tiene proyecto" en su lugar (ver la entrada v25→v26 del historial). Con la rama nueva, un único `where("assigneeIds","array-contains",uid)` ya es una consulta verificable por sí misma — coincide exactamente con esa rama de la regla — y trae de una sola vez tareas de proyecto Y personales en las que constes como responsable; la segunda consulta (`ownerId == uid`) se queda solo para el caso raro de una tarea personal tuya de la que te hayas quitado a ti mismo/a como responsable sin dejar de ser su dueño/a.

  El mismo patrón (acotar una consulta con un campo redundante pero cierto por construcción, para que Firestore pueda demostrar la regla sin tener que confiar a ciegas en un invariante de la app) se repite en tres sitios nuevos que necesitan encontrar tareas por su proyecto ADICIONAL: `subscribeToProjectTasks()` (ahora dos consultas combinadas — `projectId == X` y `extraProjectIds array-contains X` con `projectId != null` de acompañamiento — para que la Lista/Tablero/Calendario/Línea de tiempo de un proyecto muestren también las tareas que lo tienen como adicional), y `deleteProjectWithTasks()`/`saveProjectSections()` en `js/data/projects.js` (para poder LOCALIZAR esas tareas y limpiarlas, ver el punto siguiente).

  *Borrar un proyecto o una de sus secciones ya no ignora las tareas que lo tienen como adicional.* Antes de este cambio, ninguna de las dos operaciones sabía que un proyecto podía tener tareas "prestadas" de otro. Ahora: `deleteProjectWithTasks()` sigue borrando (con sus comentarios) solo las tareas que tienen ESE proyecto como PRINCIPAL, y además limpia `extraProjectIds`/`extraSections` (con `arrayRemove()` y `deleteField()`) de las que lo tenían como adicional, sin borrarlas — siguen existiendo, en su proyecto principal o como personales de alguien. `saveProjectSections()` (al eliminar una sección) repara las dos formas en que una tarea puede pertenecer al proyecto: `sectionId: null` si era su principal, o solo su entrada dentro de `extraSections` si era adicional — sin tocar la sección que tuviera en ningún otro proyecto.

  *El modal de tarea, con un selector de proyectos unificado.* `task-modal.js` se reescribió por dentro (misma ventana única de crear/editar de siempre, mismo patrón de "nada se guarda hasta Aceptar"): el antiguo desplegable "Sección" (una sola, solo para tareas de proyecto) se sustituyó por un campo "Proyectos" — una fila por cada proyecto de la tarea, con su propio desplegable de sección y un botón para quitarlo, más "+ Añadir a un proyecto" que abre un desplegable con los que faltan. Por dentro, el modal representa esto de forma unificada (`draft.projectIds[]` + `draft.sectionByProject{}}`, el primero de la lista es el principal) y solo lo reparte entre `projectId`/`sectionId`/`extraProjectIds`/`extraSections` al guardar — así el formulario no tiene que distinguir "principal" de "adicional" en ningún sitio de su propia lógica, solo Firestore ve esa distinción. El campo "Responsables" dejó de ocultarse para tareas personales (antes se forzaba siempre a `[quien la creó]` al guardar, sin poder tocarlo); los campos personalizados que se muestran son ahora la unión de los de CADA proyecto al que pertenece la tarea en ese momento más los personales de quien edita, y se repintan solos en cuanto se añade o quita un proyecto (sin tener que cerrar y reabrir la tarea para verlos aparecer).

  Quitar el último proyecto de una tarea sin querer dejarla huérfana (invisible para todo el mundo salvo un admin, si además nunca tuvo dueño/a) se resuelve en `computeOwnerIdOnSave()`: si al guardar la tarea se queda sin ningún proyecto y nunca tuvo `ownerId`, pasa a ser personal de quien la esté guardando en ese momento — mismo criterio que ya usaba "Mover a mis tareas" en las acciones masivas, reaprovechado aquí en vez de inventar un caso especial nuevo.

  *Vistas, columna "Proyecto" y acciones masivas.* `board-view.js` y `list-view.js` agrupan por sección usando `getTaskSectionForProject(t, project.id)` en vez de `t.sectionId` a secas (para que una tarea con este proyecto como adicional caiga en la columna/grupo correcto); el `moveTask()` del arrastre en Tablero recibe ahora también el proyecto sobre el que se suelta y si es principal o adicional para esa tarea en concreto, para actualizar el campo que toca. La columna "Proyecto" de Mis tareas (`my-tasks-view.js`) muestra la insignia del proyecto principal más un indicador "+N" (con los nombres al pasar el ratón) si hay adicionales, en vez de amontonar todas las insignias en una columna estrecha. En la barra de selección múltiple, "Mover a otra sección" pasa a ser consciente de cuál de las tareas seleccionadas tiene el proyecto actual como principal o como adicional (`bulkMoveToSectionInProject()`, nueva en `tasks.js`), y "Cambiar de proyecto" ahora limpia también los proyectos adicionales de las tareas que mueve (es un traslado completo, no una forma de añadir un proyecto más — para eso está el selector del propio detalle de la tarea). `openTask()` en `app.js` se simplificó de paso: ya no necesita rebuscar en qué proyecto vive una tarea antes de abrir el modal (`allProjectTasks`, un parámetro que además ya no se usaba para nada por dentro del modal desde que se quitó el campo "Bloqueada por" en la v22→v23 — se ha quitado del todo, junto con el rebuscado que solo servía para rellenarlo), porque ahora el modal recibe siempre la lista completa de proyectos (`allProjects`) y resuelve él solo, con `getTask(taskId)`, a cuáles pertenece la tarea. El buscador global (`openSearch()`) gana una deduplicación por id, porque una tarea en dos proyectos a la vez aparece una vez por cada uno en `globalTasksByProject`.

Antes de entregar: sintaxis de los 39 archivos `.js` uno por uno con `node --check`, y el mismo script de la v28→v29 que cruza cada `import` contra los `export` reales del archivo al que apunta — ambas comprobaciones limpias. La rama de Firestore (la regla nueva `uid in assigneeIds` y las consultas que dependen de ella) es la pieza de más riesgo de esta entrega por ser justo la clase de problema que ya costó tiempo antes (ver v25→v26): conviene probarla a fondo nada más publicar las reglas nuevas —créate una tarea personal, asígnasela a otra cuenta, y confirma que esa cuenta la ve y la puede completar desde su propia "Mis tareas" — antes de dar la funcionalidad por buena.

---

**v30 → v31 — Corrección de dos fallos reales de la v30, reportados nada más probarla: el desplegable de "+ Añadir a un proyecto" salía vacío, y ningún proyecto mostraba ninguna tarea.**

- *"+ Añadir a un proyecto" salía vacío.* `openAddProjectPopover()` (`task-modal.js`) envolvía la lista de proyectos en `<div class="tag-suggest">…</div>`, reutilizando sin pensarlo la misma clase del propio autocompletado de etiquetas. El problema: `.tag-suggest` lleva `position: absolute; top: calc(100% + 4px); left: 0; right: 0;` — pensada para colgar JUSTO DEBAJO de `.tag-picker` (su padre `position: relative` de siempre), no para vivir dentro de un popover nuevo como este. Al no tener ese padre relativo esperado, `.tag-suggest` se posicionaba fuera de la caja visible de `.filter-popover` (el popover en sí, con `overflow-y: auto`, quedaba prácticamente sin altura porque su único contenido pasaba a estar fuera de flujo) — la lista de proyectos existía en el DOM, con sus datos correctos, pero no se veía. Arreglado quitando ese envoltorio: los botones (`.tag-suggest__item`, que en sí mismo no tiene ningún `position` — el problema era solo el contenedor) van ahora sueltos dentro de `.filter-popover`, en flujo normal, igual que ya hacen los filtros de chips existentes. Solo `task-modal.js`; no hizo falta tocar ningún CSS.

- *Ningún proyecto mostraba ninguna tarea.* Dos causas, una de Firestore y otra de robustez del código, que se combinaban para dar el peor resultado posible (una pantalla completamente vacía, en vez de una advertencia o resultados parciales):

  1. La consulta nueva de la v30 para tareas "adicionales" (`extraProjectIds` array-contains + `projectId` != null, en `subscribeToProjectTasks()`) necesita su PROPIO índice compuesto en Firestore — distinto del que ya existía de la v25→v26 para `assigneeIds` + `projectId`. Aunque las dos tengan la misma forma (un array-contains más una comparación de `projectId`), Firestore trata cada combinación de campos como un índice aparte: tener uno no cubre al otro. Es la acción pendiente en Firebase Console — entra en el enlace exacto que aparece en el mensaje de error de la consola del navegador (`FirebaseError: The query requires an index...`), pulsa "Crear índice" y espera a que pase de "Compilando" a "Habilitado" (con datos de este tamaño, un minuto o dos suele bastar). El aviso de la v29→v30 sobre "puede pedir crear un índice, es el mismo de siempre" resultó ser impreciso — no es el MISMO, es uno nuevo y distinto — queda corregido en el apartado 7 (Limitaciones conocidas) para que no vuelva a llevar a confusión.

  2. Mientras ese índice no existe, la consulta "adicional" falla — y ahí es donde `subscribeToProjectTasks()` tenía un fallo de verdad, no solo un aviso pendiente: esperaba a que las DOS consultas (principal y adicional) terminaran de cargar antes de entregar ningún resultado (`if (!primaryLoaded || !extraLoaded) return;`), pero el callback de error de la consulta adicional nunca marcaba `extraLoaded = true` — así que, mientras esa consulta siguiera fallando, la vista se quedaba esperando para siempre algo que nunca iba a llegar, y NINGÚN proyecto mostraba NINGUNA tarea, ni siquiera las de un solo proyecto (que la consulta principal sí trae sin ningún problema, índice o no). Arreglado en `js/data/tasks.js`: el callback de error de cada consulta marca ahora su propia mitad como cargada igualmente antes de intentar avisar — así, un fallo en una de las dos (por el índice que falte, por red, por lo que sea) ya no bloquea para siempre lo que la otra sí haya podido traer; en el peor caso se ven las tareas de un solo proyecto sin las que estén ahí como adicionales, no una pantalla en blanco. Mismo cambio, por el mismo motivo y por consistencia, en `subscribeToMyTasks()` (aunque sus dos consultas no necesitan ningún índice nuevo y no deberían fallar por esto en la práctica).

Antes de entregar: mismas dos comprobaciones de siempre (sintaxis de cada `.js`, cruce de `import`/`export`), limpias. Sigue haciendo falta crear el índice nuevo en Firebase Console para ver las tareas que estén en un proyecto como ADICIONAL — sin él, la vista ya no se queda en blanco, pero esas tareas en concreto no aparecerán hasta que el índice esté listo.

---

**v31 → v32 — Dos retoques tras probar la v31: el cursor de redimensionar columna vuelve a avisar al posar el ratón encima (revierte el cambio de la v30), y el modo clásico gana su propia jerarquía de texto para la barra lateral oscura.**

- *Cursor de redimensionar columna: otra vez al posar el ratón encima, no solo al arrastrar.* Revierte exactamente el cambio de la v29→v30 (que había quitado el aviso de hover a petición explícita, por resultar confuso) — probado en uso real, resultó que sin ese aviso previo no había forma de saber que una columna se podía redimensionar sin arrastrar a ciegas primero. Un único cambio en `css/styles.css`: `.col-resize-handle` vuelve a llevar `cursor: col-resize` fijo (antes `cursor: default`), y el efecto visual de `body.is-col-resizing *` que fuerza ese mismo cursor durante el arrastre de verdad (para que no parpadee si el ratón se sale del borde de 12px mientras se arrastra rápido) se queda exactamente igual, ya que nunca dependió de este otro cambio.

- *Modo clásico: la barra lateral oscura necesitaba su propia jerarquía de texto.* La v30 solo cambió el FONDO de la barra lateral a oscuro (`--color-sidebar-bg: #2A2C2E`) sin tocar los colores de TEXTO, que en el modo clásico son los mismos oscuros del resto de la interfaz (pensados para leerse sobre el cuerpo blanco) — resultado, textos oscuros sobre fondo oscuro, ilegibles, justo lo reportado. En vez de tocar cada regla `.sidebar__*` una por una (hay más de una decena que usan `var(--color-text-hi)`, `var(--color-panel-raised)`, etc.), se aprovecha el mismo mecanismo de `--color-sidebar-bg`: esos MISMOS nombres de variable se vuelven a declarar con ámbito `:root[data-theme="classic"] .sidebar` en vez de `:root` — como una variable CSS se resuelve con el valor que tenga en el punto más cercano del árbol donde se usa, cualquier descendiente de la barra lateral (y ella misma) pasa a ver estos valores nuevos automáticamente, sin cambiar ni una sola regla existente, y todo lo que queda FUERA de la barra lateral sigue viendo los valores de siempre. El texto principal es blanco puro, tal cual se pidió; el secundario y terciario reutilizan los mismos tonos ya probados del modo oscuro (`#B4BCC2`/`#909AA0` — de sobra de contraste sobre un fondo casi igual de oscuro que `#2A2C2E`, ya que el propio modo oscuro los diseñó para un `#212325`/`#17181A` muy parecido). De paso se corrigieron también los fondos "elevado" (el resaltado al pasar el ratón por un proyecto o al tenerlo seleccionado, antes un gris CLARO que hubiera quedado como un parche fuera de lugar sobre la barra oscura) y "hundido" (el cuadro de busca de proyectos, la insignia de "Mis tareas") y el borde interno (el cuadro de búsqueda, el contador, el separador antes de "Cerrar sesión") — variaciones nuevas, calculadas a partir de la propia `#2A2C2E`, para que la barra lateral se lea como un conjunto coherente y no solo el texto suelto.

Antes de entregar: mismas dos comprobaciones de siempre, limpias. Solo CSS en los dos casos — ningún archivo `.js` cambia en esta versión.

---

**v32 → v33 — El arreglo del modo clásico de la v32 no se vio aplicado; segundo intento, más robusto. Además: un parámetro de versión en el CSS por si la causa era caché del navegador, un fallo de la línea de tiempo con tareas sin sección, y el panel de métricas.**

- *Modo clásico, segundo intento: selectores directos en vez de herencia de variables.* La v32 volvía a declarar `--color-text-hi/-lo/-faint` y otras variables con ámbito `.sidebar`, confiando en que las reglas `.sidebar__*` ya existentes (todas usan `var(--color-text-hi)`, etc.) las heredaran sin tocar ni una. Probado en uso real, no se vio aplicado — con la única excepción de "Mis tareas" (la pestaña activa), legible solo porque tiene su propio fondo claro de por medio, nada que ver con el arreglo en sí; el resto de textos de la barra lateral seguían ilegibles. No se ha llegado a confirmar con certeza si la causa era el propio navegador sirviendo el CSS de una versión anterior en caché (ver el punto siguiente) o algún matiz de cómo heredan las variables CSS dentro de elementos `<button>` — en vez de seguir dependiendo de esa herencia para averiguarlo, se ha reescrito el arreglo sin ella: cada regla `.sidebar__*` que necesitaba un color distinto en modo clásico (más de una decena — texto, fondos de hover/activo, bordes, el cuadro de búsqueda, la insignia de "Mis tareas"…) tiene ahora su propio selector `:root[data-theme="classic"] .sidebar__algo { color: #FFFFFF; }` con el valor final puesto directamente, sin ningún `var()` de por medio que dependa de heredarse correctamente. Más largo de escribir, pero no deja ningún mecanismo indirecto que pueda fallar en silencio. Mismos valores que ya se habían calculado en la v32 (texto principal blanco puro, como se pidió; el resto, tonos ya probados del modo oscuro o variaciones de la propia `#2A2C2E`).

- *Parámetro de versión en el link del CSS.* `index.html` pasa a cargar `css/styles.css?v=33` en vez de `css/styles.css` a secas. Con GitHub Pages (y cualquier hosting estático) el navegador puede quedarse con una copia en caché del CSS de una entrega anterior y no darse cuenta de que hay una nueva hasta pasado un rato, o hasta un refresco a la fuerza (Ctrl+Shift+R / Cmd+Shift+R) — que un cambio de CSS "no se vea" nada más publicarlo, aunque el archivo en sí ya esté actualizado, encaja con esto. **A partir de ahora, cada entrega nueva debe subir también en uno ese número de versión** (`?v=34`, `?v=35`…) para forzar al navegador a pedir la copia nueva sin depender de que cada persona sepa cuándo hacer un refresco a la fuerza — apuntado en el apartado 7 para no olvidarlo en la próxima.

- *Línea de tiempo de un proyecto: las tareas sin sección no aparecían.* Fallo previo a la v30 (no algo que se rompiera con el cambio a varios proyectos), recién notado al borrar secciones de un proyecto y quedarse más tareas sin ninguna. La vista construye sus grupos recorriendo `currentProject.sections` uno a uno — una tarea con `sectionId: null` (sin sección, o de una que ya no existe) no encaja en NINGÚN grupo de esa lista y desaparecía de esta vista sin más, aunque siguiera saliendo normal en Lista y Tablero (esas dos sí traían desde hace tiempo un grupo/columna "Sin sección" aparte). Arreglado en `js/app.js`: mismo criterio que Lista y Tablero ya usaban — un grupo "Sin sección" más, añadido solo si hay alguna tarea así.

- *Panel de métricas, apuntado en el apartado 6 como mejora a futuro.* Ahora hecho: tarjetas de resumen (total/completadas/pendientes/vencidas), carga por persona (barras) y listado de vencidas — ver su descripción completa un poco más arriba. Notas de implementación:
  - **Sin ninguna consulta nueva a Firestore.** Reutiliza tal cual `globalTasksByProject` (la misma bolsa de datos que ya mantenía viva la línea de tiempo global) — se extrajo un pequeño helper, `getAllProjectTasksDeduped()`, que ya usaba también `openSearch()` para evitar contar dos veces una tarea que esté en varios proyectos a la vez. Cero índices compuestos nuevos que crear, importante después de lo que costó el de la v30.
  - **Solo para admins, y solo a nivel de interfaz** — el botón de la barra lateral no se llega ni a pintar en el HTML si `userProfile.role !== "admin"` (`sidebar.js`), y `selectMetrics()` (`app.js`) es el único punto de entrada al modo, con la comprobación de rol dentro — mismo criterio que ya usan "Administrar equipo" e "Importar desde Asana". No hizo falta tocar `firestore.rules`: los datos en sí (tareas de proyecto) ya eran legibles por todo el equipo desde siempre: esto no abre ni cierra ningún permiso nuevo, solo un atajo de interfaz para quien ya podía verlos.
  - **A propósito, nunca cuenta tareas personales** — ni siquiera de forma agregada. Son privadas por diseño (solo su dueño/a y quien conste como responsable, ver `firestore.rules`), y agregar sus números en un panel que ve un admin sin que esa persona lo sepa habría sido una fuga silenciosa de esa privacidad, por pequeña que fuera.
  - Nuevo archivo, `js/views/metrics-view.js`; cambios de conexión en `sidebar.js` (botón nuevo) y `app.js` (modo `metrics`: selección, restauración al arrancar con comprobación de rol, y refresco en tiempo real reutilizando la misma suscripción de la línea de tiempo global).

Antes de entregar: mismas dos comprobaciones de siempre (sintaxis, imports), limpias — 40 archivos `.js` ahora, uno más que la v32.

---

**v33 → v34 — Las tareas sin sección pasan a salir arriba del todo (Lista, Tablero y Línea de tiempo), y nueva exportación de cualquier línea de tiempo/Gantt a Excel o PDF.**

- *"Sin sección" arriba, no al fondo.* En Lista y Tablero ya existía ese grupo/columna aparte (solo cuando hacía falta), pero al final del todo — con listas largas, una tarea huérfana de sección quedaba fuera de la vista sin hacer scroll, fácil de olvidar. Mismo cambio en las tres vistas que agrupan por sección (list-view.js, board-view.js, y la rama de línea de tiempo por proyecto en app.js, con el mismo grupo que se le añadió en la v33): ahora es lo primero que se ve, antes que cualquier sección con nombre — así se nota nada más entrar, en vez de haber que buscarlo.

- *Exportar la línea de tiempo a Excel o PDF.* Botón "⬇ Exportar" nuevo en la barra de herramientas de `timeline-view.js`, con un desplegable de dos opciones — funciona igual en la línea de tiempo de un proyecto (agrupada por sección) y en la global (agrupada por proyecto), porque las dos pasan por el mismo componente. Archivo nuevo, `js/components/gantt-export.js`:
  - **Exporta justo lo que hay en pantalla.** La función recibe los mismos `groups` que ya está pintando `renderTimelineView` en ese momento — ya vienen filtrados y ordenados por quien llama (`app.js`), así que la exportación no relee nada de Firestore ni tiene su propia noción de qué mostrar: cualquier filtro de la barra (responsable, prioridad, etiquetas, el buscador de texto…) que esté activo en pantalla se refleja en el archivo tal cual.
  - **Excel**, vía SheetJS: una fila por tarea (no filas separadoras entre secciones — así se puede ordenar o filtrar la propia hoja en Excel sin romper la agrupación, repitiendo la sección/proyecto en cada fila), con sección, tarea, responsables, prioridad, fechas, días de duración, estado y una columna de "cronograma" — una barra hecha con caracteres Unicode (█/◆) a escala del rango completo de fechas exportado, pensada para dar un vistazo visual sin depender de colorear celdas: esa parte de SheetJS es la que menos fiable resulta en su versión gratuita (la única que se puede cargar desde un CDN sin pagar licencia), así que se evitó por completo en vez de arriesgarse a un archivo que se vea distinto según qué programa lo abra.
  - **PDF**, vía jsPDF: esta vez sí un Gantt visual de verdad, apaisado, con barras de color por prioridad (rombos para los hitos) posicionadas a escala del rango completo de fechas — no del zoom que se tenga puesto en pantalla, que solo cambia el ancho de columna del Gantt interactivo y no tiene sentido llevarlo a una página de tamaño fijo. Con muchas tareas, pagina sola verticalmente, repitiendo el eje de fechas arriba de cada página nueva (no repite todavía la etiqueta de la sección/proyecto si el salto de página cae en mitad de un grupo largo — limitación menor, anotada en el apartado 7).
  - **Las dos librerías se cargan bajo demanda**, inyectando un `<script>` desde cdnjs.cloudflare.com la primera vez que se pulsa cada opción (no en cada carga de la app) — entre las dos rondan 1,3 MB, que no tiene sentido descargar quien no vaya a exportar nunca nada. Necesitan que quien las usa tenga conexión a internet en ese momento (para traer la librería la primera vez; las siguientes veces de la misma sesión ya no hace falta volver a descargarla).
  - Aviso: esta pieza en concreto no se ha podido probar de principio a fin en un navegador real (el entorno donde se preparó esta entrega no tiene salida a internet para cargar librerías externas) — la lógica se ha revisado a fondo línea por línea y encaja con la documentación de las dos librerías, pero conviene probarla nada más publicar esta versión y avisar si algo no sale como se espera.

Antes de entregar: mismas dos comprobaciones de siempre (sintaxis, imports), limpias — 41 archivos `.js` ahora.

---

**v34 → v35 — Orden de columna recordado por persona, eliminar (o reactivar) una cuenta desde "Administrar equipo", y negrita en el título de una tarea.**

- *Orden de filas recordado, por persona y por sitio.* Pulsar una columna para ordenar (Lista o Mis tareas) vivía solo en memoria — se perdía al cambiar de proyecto, ir a Mis tareas o recargar la página. Ahora se guarda en el mismo sitio que ya usan el ancho y las columnas ocultas de esa tabla, `columnPrefs.<scopeKey>.sort` (`users.js`: `setSortPref()`), así que viaja también entre dispositivos. `app.js` deja de resetear `sortState` a "sin ordenar" en `selectProject()`/`selectMyTasks()` y en su lugar lo carga de ahí (`loadSortState()`); `handleSortChange()` guarda el nuevo valor sin esperar a que termine, igual que ya hacía el arrastre de ancho de columna.

- *Eliminar (o reactivar) una cuenta, desde "Administrar equipo".* Pensado para cuando alguien deja el equipo: un botón 🗑 por cada cuenta registrada (salvo la propia) le marca `deleted: true` en su perfil (`users.js`: `setUserDeleted()`) y la mueve a una sección nueva, "Cuentas eliminadas", con un botón ↩ para reactivarla (`deleted: false`) en cualquier momento.
  - **La pérdida de acceso es real, no solo de cara a la interfaz.** Borrar de verdad la cuenta de Firebase Authentication de otra persona sigue sin poder hacerse desde el navegador (hace falta el SDK de administración, con un backend — ver el apartado 6), así que "eliminar" no borra ni el documento de Firestore ni la cuenta de Auth: si el único mecanismo hubiera sido que la propia app decidiera cerrar la sesión al notar el cambio, cualquiera con el SDK de Firestore a mano (sin pasar por la interfaz, o simplemente sin recargar la pestaña) habría seguido teniendo acceso de lectura/escritura. Para que "perder el acceso" sea cierto de verdad, `firestore.rules` tiene una función nueva, `isActiveUser()` (sesión + perfil existente + `deleted` distinto de `true`, con `.get('deleted', false)` para que ningún perfil anterior a esta versión — que no tiene el campo siquiera creado — se vea afectado) que sustituye a `isSignedIn()` en el resto de colecciones (`users`, `projects`, `tasks` y sus `comments`, `tags`, `notifications`, los `meta/asana*`): una cuenta eliminada pierde lectura Y escritura de todo, no solo de su propio perfil. `isAdmin()` pasa también a exigir `isActiveUser()`, para que un admin eliminado pierda igualmente sus permisos especiales. El propio documento de la cuenta (`users/{uid}`) protege el campo `deleted` en el `update` exactamente igual que ya protegía `role`: nadie puede tocárselo a sí mismo, ni siquiera dejándolo igual que estaba — antes de esta versión, cualquiera podía escribir cualquier campo propio salvo `role`.
  - **Se nota al momento, no solo en la siguiente carga de la página.** Como el perfil se escucha en tiempo real (`onSnapshot`), en cuanto un admin marca `deleted: true` la propia lectura deja de cumplir la regla para quien tuviera esa suscripción abierta en ese instante. `auth.js` ahora gestiona también el error de esa suscripción (antes no tenía ninguno: un fallo de lectura habría dejado la app colgada en la pantalla de carga para siempre, sin volver nunca al login) — cierra la sesión y pasa un mensaje ("Esta cuenta ha sido desactivada por un administrador.") que `app.js` muestra como aviso.
  - **Se comporta como una cuenta ficticia del importador de Asana** en todos los selectores de responsable (`task-modal.js`, `bulk-toolbar.js`): no se ofrece para tareas nuevas, pero si ya estaba asignada se sigue viendo, marcada «· Eliminado» en vez de «· Asana». De paso, se ha excluido a las cuentas ficticias de Asana de la propia lista de "Cuentas registradas" de este panel (antes se colaban ahí con un desplegable de rol que no tenía ningún sentido para ellas, al no ser una cuenta de verdad), y el buscador global (⌘K) etiqueta también a ambas en los resultados de "Personas", donde sigue siendo posible encontrarlas para llegar rápido a sus tareas pendientes.
  - **Aviso importante:** esta versión cambia `firestore.rules` — hay que volver a publicarlas (apartado 1.5) para que la pérdida de acceso funcione de verdad. El botón seguirá ahí y `deleted: true` se guardará igual aunque no se publiquen, pero la cuenta eliminada seguiría teniendo acceso por debajo hasta que se haga.

- *Negrita en parte del título de una tarea.* Lo pidió alguien del equipo, para reconocer mejor sus tareas de un vistazo. El campo de título del modal (`task-modal.js`) gana un botón "B" (también Ctrl/Cmd+B) que envuelve la selección del `<input>` entre `**dobles asteriscos**` — o la desenvuelve, si ya lo estaba —, y una vista previa debajo, visible solo si el título tiene alguna marca.
  - **A propósito sigue siendo un `<input>` normal, no un editor de texto enriquecido como el de la descripción.** Un título aparece en muchos más sitios que una descripción (ordenar, tres buscadores distintos, exportar a Excel/PDF...), y guardar HTML de verdad ahí habría significado sanear ese HTML en cada uno de esos sitios antes de pintarlo — con que uno solo se olvidara, una vía de inyección, porque cualquiera del equipo puede poner título a cualquier tarea. Con el texto siempre escapado primero, y `**` siendo la ÚNICA marca que este código interpreta (nunca una etiqueta que venga ya hecha de fuera), no hay HTML ajeno que pueda colarse en ningún sitio. Dos funciones nuevas en `utils.js`: `renderTitleHtml()` (escapa y convierte `**así**` en `<b>`, para pintarlo en pantalla) y `plainTitleText()` (quita las marcas sin más, para ordenar/buscar/exportar).
  - **Un título que fuera solo `****` sin nada escrito dentro** habría pasado la comprobación de "no está vacío" mirando la cadena en crudo (no está vacía como texto, aunque no se vea nada en pantalla) — la validación al guardar ahora usa `plainTitleText()` para evitar justo ese caso.
  - Tocados todos los sitios donde se pinta o se compara un título: `list-view.js`, `my-tasks-view.js`, `board-view.js`, `calendar-view.js`, `timeline-view.js` (la etiqueta visible en negrita; el `title=` del tooltip nativo del navegador siempre en texto plano, porque un atributo HTML no puede mostrar una etiqueta anidada), `search-modal.js` (el resultado de tarea y la propia comparación de la búsqueda), `metrics-view.js`, `gantt-export.js` (Excel y PDF, siempre texto plano) y `task-filters.js` (el buscador de texto de la barra de filtros y el orden alfabético de la columna "Nombre"); también la nota que añade `tasks.js` al combinar tareas duplicadas. Los títulos de subtareas y los sitios que solo copian un título ya existente sin que nadie lo teclee de nuevo (duplicar tarea, el importador de Asana) se quedan como estaban, sin cambios — no hacía falta tocarlos.

Antes de entregar: mismas dos comprobaciones de siempre (sintaxis, imports), limpias — 41 archivos `.js`, los mismos que la v34 (ningún archivo nuevo esta vez, todo cambios dentro de archivos ya existentes).

---

**v35 → v36 — Fusión de dos v35 hechas en paralelo, en dos chats distintos, sobre la misma v34: la de este chat (orden de columna recordado, eliminar/reactivar cuenta, negrita en el título — la entrada de arriba) y la de otro (Excel de la línea de tiempo con la plantilla real de la empresa, y notificaciones al asignar una tarea). El resultado final tiene las cinco funciones a la vez, ya integradas entre sí.**

Mismo método que ya dejó documentado la fusión de la v28→v29: diff de tres vías contra la v34 común (el ancestro compartido de ambas ramas) — un archivo que solo tocó una rama se lleva tal cual; uno que tocaron las dos hay que fusionarlo a mano, línea por línea.

- *Casi ningún choque de verdad.* De los archivos que tocaron ambas ramas (`app.js`, `bulk-toolbar.js`, `gantt-export.js`, `task-modal.js`, `timeline-view.js`, además de `README.md`, `css/styles.css`, `firestore.rules` e `index.html`, que tocaba casi cualquier entrega), en la práctica cada rama había cambiado una parte distinta del archivo — la única excepción de verdad fue el bloque de "asignar a una persona" de `bulk-toolbar.js` (ver más abajo). Dos archivos nuevos de la otra rama, `js/data/notifications.js` y `js/components/notification-bell.js`, se incorporan tal cual; igual la plantilla `assets/gantt-template-martech.xlsx`.

- **Bug encontrado en `firestore.rules` al comparar: dos bloques `match /notifications/{notifId}` a la vez.** La otra rama añadió el bloque de permisos real de notificaciones (exigiendo `fromUserId == request.auth.uid` al crear, para que nadie pueda suplantar de quién viene un aviso) pero no borró el bloque antiguo que ya traía la v34 desde que esa colección era solo "reservado para fase 2" (permisivo, sin esa exigencia). En Firestore, cuando dos bloques `match` coinciden con la misma ruta, las reglas se combinan con OR — así que el bloque antiguo, más laxo, seguía anulando en la práctica la protección que el nuevo intentaba añadir: cualquier cuenta con sesión podía haber creado una notificación a nombre de cualquier otra persona como remitente, sin que la exigencia de `fromUserId` sirviera de nada. Arreglado fusionando en uno solo: mi `isActiveUser()` (para que una cuenta eliminada tampoco pueda crear ni leer notificaciones) más su exigencia de `fromUserId`.

- *Excel con la plantilla real de la empresa (a partir del `.xlsm` de referencia que se pasó en la otra conversación).* Lo más laborioso de esa rama, con diferencia. La plantilla colorea las barras del Gantt con formato condicional de Excel (10 reglas, una por color, comparando sección y rango de semanas de cada tarea contra la fila de números de semana de la cabecera) — la versión gratuita de SheetJS no reproduce con fiabilidad ni el formato condicional ni el relleno de celda al construir un libro nuevo desde cero. La solución fue no construir nada desde cero: partir del `.xlsx` real y cambiarle solo los valores, directamente sobre su XML interno (`.xlsx` es un `.zip` con XML dentro — JSZip, cargada bajo demanda, abre el zip, localiza `xl/worksheets/sheet1.xml` y sustituye celda a celda cliente/proyecto/fecha y, por cada tarea, nombre/semanas/color, conservando intacto el atributo de estilo que ya tenía cada celda). La plantilla original solo traía 10 filas ya formateadas — se amplió a 50 (con `openpyxl`, en el entorno de aquella conversación) replicando el estilo exacto y extendiendo el formato condicional a las filas nuevas; en el proceso apareció un fallo real de `openpyxl` (una fila se guardaba con la mitad de sus celdas sin escribir, detectado comparando el XML final celda a celda) que se reparó clonando el patrón de una fila ya verificada. El resultado se probó simulando en Node.js el mismo código de relleno que usa el navegador, y convirtiendo el Excel de salida a PDF con LibreOffice para verlo tal cual lo vería quien lo abra — con un proyecto de prueba de 12 tareas, cabecera, colores por sección y posición de las barras salieron como se esperaba. Límites: hasta 50 tareas con fecha y 40 semanas de rango dentro de un mismo año natural (apartado 7) — si no cabe, avisa por qué y cae al Excel genérico en vez de dejar a quien exporta sin ningún archivo; la línea de tiempo global siempre usa el genérico, porque la plantilla da por hecho un único cliente/proyecto. El archivo que sale es un `.xlsx` normal sin macros — el `.xlsm` original sí las traía, pero mantenerlas habría añadido riesgo sin aportar nada a la exportación automática, y habría disparado el aviso de seguridad de macros de Excel sin necesidad. Para esta fusión: los 3 sitios donde esa función pinta el nombre de una tarea (el Excel genérico, la celda de la plantilla, y la etiqueta del PDF) usan ahora `plainTitleText()` en vez del título en crudo, para que una tarea con negrita (v35, la de este chat) no saque los asteriscos literales en el documento exportado.

- *Notificaciones: aviso al asignarte una tarea.* Campana 🔔 fija arriba a la derecha, con panel desplegable (apartado 3). Notas de la otra rama, con dos puntos de integración añadidos en esta fusión (marcados):
  - Colección propia `notifications/{id}`, privada — solo la lee/gestiona quien conste en `userId`; cualquier cuenta puede crear una a nombre de otra (así funciona asignar), identificándose como `fromUserId` (ver el bug de reglas de más arriba).
  - Sin índice compuesto nuevo, a propósito: la consulta de "mis notificaciones" es un único `where("userId","==",uid)` sin `orderBy()` — el orden (más reciente primero) se hace en el cliente, mismo motivo y mismo patrón que ya usa `tasks.js` desde la v30 para no depender de un índice.
  - Se detecta comparando quién era responsable ANTES de guardar con quién lo es DESPUÉS — solo avisa a quien se añade de nuevas, nunca a quien ya lo era, y solo si el guardado ha terminado con éxito. Tres sitios lo disparan: el detalle de una tarea (`task-modal.js`), "Asignar a alguien" y "Agregar colaboradores…" de la selección múltiple (`bulk-toolbar.js`) — **el único choque de línea de verdad de toda la fusión** estaba en el primero de estos dos: las dos ramas habían tocado el mismo bloque del selector de "asignar a una persona" (yo, para quitarle una etiqueta "· Asana" que ya no podía salir nunca, al filtrar también a los eliminados; la otra rama, para añadir el aviso). Se resolvió quedándose con la lógica de aviso completa y aplicándole encima mi etiqueta simplificada.
  - **Integración con la negrita del título (v35, este chat):** `notification-bell.js` pintaba el título de la tarea con `escapeHtml()` a secas — un título con negrita se habría visto con los asteriscos literales dentro del aviso. Cambiado a `renderTitleHtml()`, igual que en cualquier otro sitio donde se muestra el título de una tarea.
  - El modelo deja sitio a más tipos de aviso en el futuro (campo `type`, hoy siempre `"task_assigned"` — apartado 6). Sin limpieza automática de notificaciones antiguas todavía (apartado 7).

- **Aviso importante, doble esta vez:** esta versión toca `firestore.rules` por los dos lados (mi `isActiveUser()` de la v35 de este chat, más el bloque de notificaciones ya corregido) — hay que volver a publicarlas (apartado 1.5) para que tanto "eliminar usuario" como las notificaciones funcionen de verdad. Hasta que no se haga, la campana puede fallar al crear avisos (bloqueada por las reglas viejas) y una cuenta eliminada seguiría teniendo acceso por debajo.

Antes de entregar: mismas dos comprobaciones de siempre, ahora sobre el código ya fusionado (no antes de fusionar) — sintaxis de los 43 archivos `.js` (41 de la v35 de este chat + los 2 nuevos de la otra: `notifications.js`, `notification-bell.js`) y el cruce de cada import contra los export reales, limpias las dos. La plantilla Martech no se ha podido volver a probar de principio a fin en este entorno (sin salida a internet para SheetJS/JSZip) — la otra conversación sí la probó con datos reales antes de pasarla, como se explica más arriba; las notificaciones tampoco se han podido probar contra Firestore de verdad (hace falta una sesión iniciada real y las reglas ya publicadas) — conviene confirmar ambas en cuanto se publiquen las reglas nuevas.

---

**v36 → v37 — Dos ajustes a la campana de notificaciones de la v36: dejaba de solaparse con el botón "+ Nueva tarea", y su icono pasa de emoji a un SVG con los colores de la propia app.**

- *La campana se solapaba con "+ Nueva tarea".* Causa: `.topbar` tiene `padding: 0 24px`, y `.notif-bell` (posicionada encima por CSS, no dentro del propio topbar — ver el porqué en su comentario) estaba anclada a `right: 24px` — exactamente el mismo borde en el que ya termina el botón "+ Nueva tarea", el último elemento del `flex` del topbar. Arreglado reservándole hueco de verdad: `.topbar` pasa a `padding: 0 74px 0 24px` (los 50px de más son 34 del ancho de la campana + 16 de aire, el mismo `gap` que ya usa el topbar entre sus propios elementos), así que ahora el botón termina antes y deja sitio limpio a la derecha para la campana, sin que ninguno de los dos necesite saber nada del otro.
- *El emoji 🔔 pasa a ser un SVG.* Un emoji no se puede recolorear con CSS: salía siempre con su amarillo de serie, sin distinguir modo claro/oscuro/clásico ni reaccionar al `:hover` como el resto de iconos del topbar. El SVG nuevo (un contorno de campana simple, dos `<path>`) usa `stroke="currentColor"`, así que toma el mismo `color` que ya tenía el botón (`--color-text-lo`, y `--color-text-hi` al pasar el ratón) — cambia de color con el tema exactamente igual que el resto de la interfaz, sin ninguna regla nueva para eso. El punto de "sin leer" (`.notif-bell__dot`) se ha reposicionado un poco (de `top:13px` a `top:18px`) para quedar bien pegado a la esquina del icono nuevo, que no ocupa exactamente el mismo espacio que tenía el emoji.

Antes de entregar: sintaxis y cruce de imports limpios (no se ha tocado ningún `.js`, solo `index.html` y `css/styles.css` — 43 archivos `.js`, sin cambios respecto a la v36).

---

**v37 → v38 — La exportación a Excel con la plantilla Martech (v36) no se había vuelto a probar de principio a fin desde que se creó (la propia v36 lo avisaba: "no se ha podido volver a probar... en este entorno"), y en la práctica tenía CUATRO fallos distintos a la vez. Esta versión parte del `CRONOGRAMA.xlsm` real que ha pasado quien encarga el proyecto (con datos de un cliente real ya rellenados) para diagnosticarlos uno a uno contra el archivo de verdad, y los arregla todos.**

Diagnóstico, comparando byte a byte el `.xlsm` real contra la plantilla que traía el proyecto (`assets/gantt-template-martech.xlsx`):

- **Al formato condicional de la plantilla le faltaban las 10 reglas que pintan cada color** — solo conservaba las 2 de bandeado (columnas pares/impares). Por eso ninguna tarea se coloreaba nunca en ningún export, fuera cual fuera el color elegido en la columna E: la plantilla nunca tuvo cómo interpretarlo.
- **La plantilla llevaba horneadas de fábrica las 10 tareas de ejemplo de ese cliente real** (`CRONOGRAMA.xlsm`) en las filas 13-22, con su nombre de proyecto y su cabecera. El código anterior solo escribía sobre las filas que hicieran falta para las tareas reales (`sorted.forEach` con `MARTECH_FIRST_TASK_ROW + i`) — si un proyecto exportado tenía menos de 10 tareas con fecha, las filas sobrantes se quedaban tal cual, coladas en el Excel de cualquiera que exportara: el nombre de ese cliente, su pedido y sus tareas de verdad, dentro del archivo de otro proyecto distinto.
- **Le faltaban los dos botones ("Preparar para envío" / "Modo edición") y sus macros.** La plantilla anterior se generó ampliando el `.xlsm` original a 50 filas con `openpyxl` sin pasarle `keep_vba=True` — sin ese parámetro, openpyxl descarta el proyecto VBA (y con él los botones, que dependen de esas macros) al guardar, sin ningún aviso de que lo ha hecho.
- **La cabecera de semanas/meses (filas 6/7/11/12, fórmulas de Excel) se veía con el año y la semana de ejemplo, no con los que el código acababa de escribir en D9/D10.** La plantilla ya llevaba `fullCalcOnLoad="1"` (para que Excel recalcule solo al abrir el archivo), pero no todos los programas lo respetan igual — comprobado con la conversión en línea de comandos de LibreOffice, que sigue mostrando el valor cacheado viejo pese a ese flag. Como además la propia plantilla horneada tenía de ejemplo la semana 8 (no la 2 ni cualquier otra que tocara en cada export), esto pasaba desapercibido con las semanas iniciales bajas y solo se notaba de verdad con datos reales: cualquier tarea con semana de inicio menor que la del ejemplo horneado simplemente no llegaba a pintarse (caía "antes" de donde la cabecera decía que empezaba el rango visible).

Solución, plantilla nueva (`assets/gantt-template-martech.xlsm`, sustituye al `.xlsx` anterior):

- Se generó vaciando el `.xlsm` real celda a celda **directamente por su XML** (`zipfile` + un `str.replace` dirigido, sin `openpyxl` ni ninguna otra librería de Excel de por medio) — deja B13:E22, F2:F5, D9, D10 y F10 en blanco (el código los rellena siempre) y añade `fullCalcOnLoad="1"` al libro, sin tocar ni una fila, celda, macro o botón más. Verificado con `sha256` que `vbaProject.bin`, los dos `ctrlProps` (botones), el `vmlDrawing` y el logo salen **idénticos byte a byte** al archivo real que se pasó de referencia.
- El código NUNCA reescribe la plantilla de fábrica: la sube tal cual desde `assets/`, y solo la toca en cada exportación individual, en memoria, con JSZip.

Solución, `js/components/gantt-export.js` (`checkMartechTemplateFit`, `buildMartechExcel` y sus funciones auxiliares, reescritas):

- **Ya no fuerza ningún número de estilo.** Antes, `setInlineStringCell`/`setNumberCell` recibían un estilo fijo (18/7/3/3, válido solo para las filas 13-22 del `.xlsm` original) y se lo aplicaban a la celda pasara lo que pasara. Ahora leen el estilo que la celda YA tenga en la plantilla y lo conservan tal cual — así ninguna fila puede acabar con un formato que no era el suyo, venga de donde venga esa fila (nativa o clonada, ver debajo).
- **Ya sabe qué filas son "OBSERVACIONES" (24) y qué fila es un hueco fijo de la plantilla (26), y las salta siempre** al repartir tareas entre filas (`martechRowForIndex`) — antes no existía esta función; se limitaba a sumar el índice de la tarea a la primera fila, sin tener en cuenta que la propia plantilla real reserva esas dos filas para otra cosa.
- **Vacía explícitamente cada fila nativa que no se use en un export dado** (no solo las que hagan falta) — así, aunque la plantilla en blanco se corrompiera o alguien la editara a mano en el futuro, nunca podría colarse el resto de un cliente distinto en el Excel de otro.
- **Ya no depende de que quepan en un número fijo de filas.** La plantilla trae de fábrica 19 filas de tarea ya formateadas y con su formato condicional listo (13-23, 25 y 27-33 — `MARTECH_NATIVE_SLOTS`); si hacen falta más, el código clona la fila 27 tal cual (capturada ANTES de escribir ningún dato, para no arrastrar nunca texto de una tarea real en el clon) y extiende el formato condicional, la validación de la columna E, el `dimension` de la hoja y el área de impresión hasta la última fila que se use. Probado hasta 28 tareas (9 filas clonadas, hasta la 42) sin ningún problema.
- **F10 (el rótulo "AÑO 2026" que se ve encima de la cuadrícula) se sincroniza con D9** — en la plantilla es texto fijo, no una fórmula, así que si nadie lo toca a mano se queda con el año de cuando se rellenó la plantilla por última vez.
- **La cabecera de fechas se recalcula a mano**, sin fiarse de que el programa que abra el archivo lo haga solo (`computeMartechHeaderValues`, que replica en JavaScript exactamente las mismas fórmulas de la plantilla — semana con su acarreo al pasar de 52/53, mes a partir del jueves ISO de cada semana, y la etiqueta de mes que solo se repite cuando cambia) — y deja ya escrito ese resultado como valor cacheado de cada una de las 160 celdas con fórmula (filas 6/7/11/12 × 40 columnas), sin tocar la fórmula en sí. Así se ve bien tanto si el visor recalcula como si no.
- **Bug encontrado y arreglado durante esta misma reescritura, antes de entregarla:** la primera versión de `computeMartechHeaderValues`/`setFormulaCachedValue` corrompía la fórmula original al escribir el valor cacheado — `$D$10` se quedaba en `$D s="1"0`. Causa: `String.replace(patrón, texto)` interpreta `$1`, `$&`, etc. dentro del texto de reemplazo como referencias especiales, y las fórmulas de esta plantilla contienen literalmente `$D$9`/`$D$10`. Se detectó comparando el XML de salida antes/después de recalcular la cabecera, no a simple vista. Arreglado pasando una función en vez de un string como segundo argumento a `replace()` en las seis funciones del archivo que insertan texto dinámico (`setInlineStringCell`, `setNumberCell`, `clearCell`, `ensureMartechRow`, `extendMartechSheetRanges`, `setFormulaCachedValue`, y el ajuste del área de impresión) — una función de reemplazo nunca interpreta su valor de vuelta como patrón especial, pase lo que pase dentro. Sin este segundo arreglo, cualquier título de tarea con un `$` (p. ej. una cifra de coste) habría corrompido igual la celda.
- **El archivo que se descarga ahora es un `.xlsm`, no un `.xlsx`** (`${nombre}-cronograma.xlsm`, con el tipo MIME `application/vnd.ms-excel.sheet.macroEnabled.12`) — tiene que serlo para que Excel abra los botones y las macros; con la extensión antigua, Excel las habría descartado igual al abrir el archivo, aviso de seguridad de macros incluido.

Verificación (dado que el entorno donde se prepara esta entrega no tiene salida a redes de Microsoft, solo se ha podido probar contra LibreOffice, no contra Excel real — aunque el diseño ya no depende de que ningún programa concreto recalcule nada, ver el punto de la cabecera):

- Arnés en Node.js (`jszip`, sin ningún cambio respecto al código real: se copian los mismos archivos que van en el ZIP y solo se añade `export` a dos funciones para poder llamarlas desde la prueba) con tres escenarios — 12 tareas (caben en las filas nativas), 28 tareas (obliga a clonar 9 filas nuevas) y 3 tareas (para comprobar que las filas nativas sin usar quedan de verdad en blanco). Los tres, convertidos a PDF con LibreOffice en línea de comandos y revisados visualmente: la cabecera coincide con D9/D10, cada barra empieza en su semana y con su color de sección, y las filas clonadas (34-42) se ven exactamente igual que las nativas.
- `sha256` de `vbaProject.bin`, los dos `ctrlProps` y el `vmlDrawing` contra el archivo real, en los tres Excel de salida de esas pruebas: idénticos byte a byte en los tres casos.
- Detalle curioso, no un bug de este código: la fila 23 de la plantilla real (una tarea "de más" tras el bloque original de 10) tiene un estilo ligeramente distinto al de las filas 13-22 — el número de duración y de semana de inicio se ven algo más pegados entre sí, sin la misma separación visual. Es así en el `.xlsm` original que se pasó de referencia, no algo que haya cambiado esta versión (que nunca fuerza estilos, ver arriba) — se deja constancia aquí por si se nota al mirar un export y conviene saber que no es nuevo.

Antes de entregar: sintaxis de los 43 archivos `.js` y cruce de imports limpios (solo cambia `gantt-export.js`, ninguno más). `css/styles.css?v=38` (sin cambios de contenido en el CSS esta vez, pero se sube igual el número por convención — ver apartado 7). No hace falta tocar `firestore.rules` ni ningún índice: este cambio no toca Firestore en absoluto.

---

**v38 → v39 — A partir de haber probado ya la v38 con datos reales: el estilo "de más" de la fila 23 (avisado como detalle curioso en el historial de la v38) corregido en la plantilla, un color por fila probado y descartado (se queda como estaba, por sección), y una revisión a fondo de las notificaciones — límite visible más bajo, se distinguen a simple vista las leídas de las que no, se ve el proyecto de la tarea, y la colección se autolimpia sola.**

- **Fila 23 (y, sin que se hubiera notado todavía, las filas 25 y 27-33) con un estilo distinto al de las filas 13-22.** No era un bug de código — el propio `.xlsm` real que sirvió de referencia ya traía esas filas "de más" con otro estilo (sin la misma alineación centrada, con una fuente distinta) en vez de con el de las 10 originales. Arreglado en la plantilla (`assets/gantt-template-martech.xlsm`), no en el código: se igualó el atributo `s` (estilo) de B/C/D/E en esas nueve filas al que ya tenían B13/C13/D13/E13 (`s="18"`/`"7"`/`"3"`/`"3"`), editando solo esos atributos en el XML — verificado que el resto del archivo (macros, botones, logo, formato condicional, validaciones) sigue siendo idéntico byte a byte al de la v38, célula por célula, salvo esos 35 atributos (uno de los 36 ya tenía el valor correcto de antes: B25 ya usaba el estilo bueno). Como `buildMartechExcel` clona la fila 27 tal cual esté en la plantilla en ese momento para las tareas 20ª en adelante (ver v38), arreglar la plantilla arregla también, sin tocar ni una línea de código, el aspecto de todas las filas que el código vaya a clonar en el futuro.
- **Color por fila, probado y descartado.** Se probó a asignar un color del ciclo de 10 tarea por tarea (en vez de uno por sección) para que se distinguiera mejor cada fila — pero, tras probarlo con datos reales, se prefirió volver a como estaba: un color por SECCIÓN, por orden de primera aparición entre las tareas ya ordenadas cronológicamente (`colorFor()` y el `Map` que lleva la cuenta, que se habían quitado en el intento, están de vuelta tal cual estaban en la v38).

Notificaciones (`js/data/notifications.js`, `js/components/notification-bell.js`, `css/styles.css`) — repasadas de arriba abajo a partir de lo comentado sobre el límite de la v37:

- **El panel ahora enseña 20 como mucho, no 30** (`NOTIF_VISIBLE_LIMIT`, una constante compartida entre los dos archivos en vez de un número suelto en cada uno, para que no se puedan quedar descuadrados entre sí).
- **Leídas y sin leer ya se distinguen a simple vista, no solo por el puntito.** Las que quedan por leer llevan ahora también el fondo teñido de `--color-signal-soft` (la misma variable que ya usan los botones activos de la barra de formato — se reutiliza en vez de inventar un color nuevo, para que respete igual los tres temas claro/oscuro/clásico); las leídas pierden ese fondo y además el texto pasa a `--color-text-faint`, quedando visualmente en un segundo plano.
- **Se ve el proyecto de la tarea.** El aviso ahora guarda también `projectName` (además del `projectId` que ya guardaba) — copiado tal cual estaba al crear el aviso, igual que ya se hacía con `taskTitle`, para que no cambie si el proyecto se renombra después — y el panel lo enseña junto al título de la tarea (nada si la tarea es personal, sin proyecto). Threading de `projectName` (buscando el nombre por `projectId` contra la lista de proyectos ya cargada) y de `teamMembers` (ver el siguiente punto) añadido en las cuatro llamadas a `notifyNewAssignees` (dos en `task-modal.js`, dos en `bulk-toolbar.js` — una de ellas obligó a pasar `projects` dos niveles más abajo, desde `renderBulkToolbar` hasta `openCollabPopover`, a través de `openMoreMenu`).
- **Los usuarios ficticios del importador de Asana (`isImported`) ya no reciben notificaciones.** `notifyNewAssignees` recibe ahora también `teamMembers` y descarta de los destinatarios a cualquiera marcado como `isImported` antes de crear nada. En la práctica, los cuatro sitios desde donde se llama ya impedían elegir un ficticio como responsable NUEVO a través de su selector de personas correspondiente (por eso probablemente no se había notado como un problema activo) — pero la regla ahora vive en el sitio correcto (la propia función que crea notificaciones) en vez de depender de que ningún otro selector cambie de comportamiento el día de mañana.
- **La colección se autolimpia sola, sin ninguna acción manual.** Cada vez que a alguien le llega una actualización en vivo de sus propias notificaciones (`subscribeToNotifications` — es decir, cada vez que recibe una nueva o cambia alguna suya), se comprueba su lista, ya en memoria, sin ninguna lectura aparte: si tiene más de 20 SIN LEER, las más antiguas de ese exceso se marcan leídas solas (nunca se van a poder pintar ni abrir a mano, porque el panel solo enseña las 20 más recientes en total); y si en total tiene más de 20, se BORRAN las leídas más antiguas hasta dejar como mucho 20 (nunca una que siga sin leer). Verificado el algoritmo con un pequeño script en Node contra siete escenarios sintéticos (desde "20 exactas, no toca nada" hasta "50 con solo 5 leídas") antes de darlo por bueno.
  - Importante por qué vive AQUÍ y no en `notifyNewAssignees`: la regla de Firestore exige que quien borra o actualiza una notificación sea su propio dueño (`resource.data.userId == request.auth.uid`). Quien ASIGNA una tarea no tiene permiso para tocar las notificaciones de la persona a la que se la asigna, así que la limpieza no puede dispararse al crear el aviso — tiene que vivir en la sesión de quien lo RECIBE, y `subscribeToNotifications` (que ya corre ahí, en vivo, todo el rato que la app está abierta) es el sitio natural.
  - No hace falta ningún cambio en `firestore.rules`: el `delete`/`update` de la propia notificación ya estaba permitido desde que se creó la colección (v36), y no hay ninguna validación de campos que bloquee guardar `projectName`.

Antes de entregar: sintaxis de los 43 `.js` y cruce de imports limpios (`gantt-export.js`, `notifications.js`, `notification-bell.js`, `task-modal.js`, `bulk-toolbar.js`). Repetido el arnés de Node.js + LibreOffice de la v38 sobre el Excel (color por sección confirmado visualmente, fila 23 sigue viéndose bien). `css/styles.css?v=39`.

---

**v39 → v40 — Aviso emergente al asignar una tarea (además del puntito de la campana), y un bug real: borrar una tarea individual desde Lista podía decir "Tarea eliminada" sin haberla eliminado de verdad.**

- **Aviso emergente ("toast") al recibir una tarea nueva con la app ya abierta.** Más llamativo que el toast normal de confirmaciones ("Tarea guardada" y similares): icono 🔔, dos líneas (quién te la asignó + título, y el proyecto si tiene uno), más tiempo en pantalla (6 s en vez de los 3,2 s de siempre) y clicable — lleva directo a la tarea, marcándola como leída de paso. Si llegan varias de golpe (una asignación masiva a ti desde la selección múltiple, por ejemplo), un único aviso agrupado ("Tienes N tareas nuevas asignadas") que lleva al panel en vez de a una tarea en concreto.
  - `subscribeToNotifications` (notifications.js) admite ahora un tercer argumento opcional, `onNewNotifications`, que se dispara SOLO con notificaciones genuinamente nuevas — nunca con el `snapshot` inicial. Ojo con esto si se toca esta función más adelante: el primer snapshot que entrega Firestore siempre trae TODO el resultado ya existente marcado como `"added"` en `docChanges()`, así que hace falta descartar explícitamente esa primera vuelta con un flag (`isFirstSnapshot`) — si no, cualquiera que abra la app con notificaciones pendientes vería un aviso emergente por cada una de golpe, en vez de solo por las que lleguen de verdad a partir de ahí. El punto de la campana, en cambio, SÍ debe reflejar las que ya hubiera de antes, así que ese filtro no toca al `callback` de toda la vida, solo al nuevo.
  - Conectado en `app.js`: al recibir `onNewNotifications`, llama a `showAssignedTaskToast()` (nueva, en `notification-bell.js`) con `onOpenTask` (la misma función de siempre) y `onOpenPanel` (abre el panel de la campana, para el caso de varias de golpe).
- **Bug: "Tarea eliminada" aunque no se hubiera eliminado.** En Lista, el menú contextual de una tarea (clic derecho) llamaba a `deleteTask(task.id)` sin esperar su resultado, y a continuación, siempre, incondicionalmente, mostraba "Tarea eliminada." — si Firestore rechazaba el borrado (por ejemplo, por no tener permiso — ver el apartado 7, "Quién puede borrar una tarea"), la promesa de `deleteTask` quedaba rechazada sin que nadie la recogiera (un *unhandled rejection*, visible solo en la consola del navegador) y el toast de éxito salía igual, mintiendo sobre lo que había pasado de verdad. La tarea seguía ahí, sin ningún aviso que lo explicara. Arreglado con el mismo patrón que ya usa correctamente `handleDelete` en `bulk-toolbar.js`: esperar el resultado (`await`), y solo entonces decidir qué toast mostrar — "Tarea eliminada." si de verdad se ha borrado, "No se pudo eliminar (sin permiso)." si Firestore lo rechaza. Es el único sitio de todo el proyecto que borraba una tarea suelta sin pasar por `bulkDeleteTasks` (que ya gestionaba esto bien) — revisado el resto de vistas para confirmar que no se repite en ningún otro sitio.
  - **Pendiente de decidir, no tocado en esta versión:** por qué la persona no tenía permiso para empezar — es por diseño, no un bug de las reglas (`firestore.rules`, comentario junto a `allow delete` de `tasks`): quien es responsable de una tarea PERSONAL (sin proyecto) puede leerla y editarla igual que su dueño/a, pero no puede BORRARLA — eso se queda reservado para quien la creó (`ownerId`/`createdBy`) o un/a admin. No tiene que ver con ser o no admin en sí (esa es solo una de las cuatro maneras de tener permiso para borrar; ser el dueño/a es otra, independiente de admin) — tiene que ver con que ser responsable de una tarea nunca ha dado permiso para borrarla, solo para trabajarla. Si se decide ampliarlo, es un cambio en `firestore.rules` (añadir `(request.auth.uid in resource.data.assigneeIds)` a la rama de `allow delete`), sin tocar nada de código — un cambio pequeño pero de los que conviene decidir a propósito, no dar por hecho.

Antes de entregar: sintaxis de los 43 `.js` (`notifications.js`, `notification-bell.js`, `app.js`, `list-view.js`). `css/styles.css?v=40`. No toca `firestore.rules` (la pregunta de permisos de arriba queda pendiente, sin aplicar).

## 4. Estructura del proyecto

```
index.html                 Pantalla de login/registro + estructura de la app
css/styles.css              Todo el diseño
assets/                     Logo de Chusy (principal), de Martech Corporation (crédito) y la plantilla real de Excel de la empresa (gantt-template-martech.xlsm, con sus macros y botones — ver apartado 3 "Exportar la línea de tiempo" y el historial de la v38)
js/
  firebase-config.js        ← AQUÍ pegas tu configuración de Firebase
  firebase-init.js           Inicializa Firebase (auth, db)
  auth.js                    Registro / inicio de sesión / roles
  theme.js                    Modo claro/oscuro/clásico: aplica el tema (atributo `data-theme` en `<html>`) y lo cachea en este navegador — la cuenta (`users/{uid}.theme`) es la fuente de verdad, esto solo evita el parpadeo mientras carga
  utils.js                   Fechas, avatares, contraste de color, helpers
  data/
    projects.js               CRUD de proyectos (incluye borrado en cascada — también limpia, sin borrarlas, las tareas que solo tienen ese proyecto como adicional — archivado y gestión de secciones: crear/renombrar/eliminar, con reasignación de tareas huérfanas a "sin sección", tanto si el proyecto es su principal como si es uno adicional)
    tasks.js                   CRUD de tareas (proyecto y personales, y en varios proyectos a la vez — ver `extraProjectIds`/`extraSections` en el modelo de datos) + operaciones en lote para la selección múltiple de Lista y Mis tareas (mover, cambiar de proyecto, mover a mis tareas, asignar, fechas, completar, combinar duplicadas, borrar)
    tags.js                     Registro compartido de etiquetas (nombre + color)
    comments.js                  Comentarios de una tarea
    notifications.js              Notificaciones (crear al asignar, marcar leída/todas, suscripción a las propias, autolimpieza — ver apartado 7)
    users.js                      Perfil de usuario (Firestore), configuración del equipo y eliminar/reactivar cuentas
    asana-import.js                Importador de Asana: lee el export, resuelve equivalencias y escribe/reescribe en Firestore
  components/
    sidebar.js                  Proyectos + Mis tareas + Línea de tiempo + Archivo + Métricas + buscador + menú de cuenta
    topbar.js                    Selector de vista + nueva tarea
    project-modal.js             Crear proyecto
    edit-project-modal.js         Editar nombre/icono/color de un proyecto existente
    project-appearance-picker.js  Selector de icono+color compartido (crear y editar)
    custom-fields-modal.js        Definir campos personalizados (lista/número/texto)
    task-modal.js                  Formulario único de tarea (crear/editar), con selector de proyectos múltiples (cada uno con su propia sección) y responsables — funciona igual para tareas de proyecto y personales
    rich-text-editor.js            Editor de texto enriquecido reutilizable (barra de herramientas + menú "/"), lo usa el campo Descripción del modal de tarea
    context-menu.js                Menú contextual reutilizable (clic derecho)
    filter-bar.js                   Barra de filtros reutilizable, con cuadro de búsqueda por texto
    gantt-export.js                  Exportar una línea de tiempo/Gantt a Excel (plantilla real de la empresa para un proyecto suelto, genérico si no cabe o es la vista global) o PDF — lo usa timeline-view.js, carga sus librerías (SheetJS, JSZip, jsPDF) bajo demanda desde un CDN
    notification-bell.js             Campana de notificaciones: punto de sin-leídas y panel desplegable
    search-modal.js                  Buscador global (⌘K / Ctrl+K)
    account-modal.js                  "Mi cuenta": nombre, rol, apariencia (claro/oscuro/clásico) y cambio de contraseña
    team-admin-modal.js                Panel de admin: roles del equipo, eliminar/reactivar cuentas y dominios permitidos
    asana-import-modal.js               Panel de admin: importar desde Asana y mapear personas
    reset-password-modal.js              "He olvidado mi contraseña" (pantalla de login)
    table-columns.js                     Ancho, visibilidad y orden de filas de tabla (Lista y Mis tareas) por ámbito (proyecto o Mis tareas); orden de columnas por arrastre de cabecera, único y global por persona — todo se guarda en su perfil
    bulk-selection.js                     Controlador de selección múltiple de tareas (Ctrl/Cmd+clic, Shift+clic) — lo usan Lista y Mis tareas, cada una con su propia instancia
    bulk-toolbar.js                        Barra flotante de acciones masivas sobre la selección (mover, cambiar de proyecto, mover a mis tareas, asignar, fechas, completar, combinar duplicadas, convertir en hitos...) — la usan Lista y Mis tareas
    sections-modal.js                       Crear/renombrar/eliminar las secciones de un proyecto (columnas del Tablero, agrupaciones de la Lista)
    celebration.js                          Confeti al completar tareas: chispazo pequeño al marcar una, y recompensa grande (confeti por toda la pantalla + mensaje) al completar varias de golpe por selección múltiple, varias seguidas y rápido, o al cruzar un hito de tareas completadas en el día
  task-filters.js             Filtrado y ordenación de tareas (compartido por todas las vistas)
  views/
    list-view.js                  Vista de Lista: tabla ordenable (+ menú contextual, selección múltiple y su barra de acciones masivas)
    board-view.js                  Vista de Tablero (Kanban con drag & drop)
    calendar-view.js                Vista de Calendario (barras de duración + hitos)
    timeline-view.js                 Línea de tiempo/Gantt (por proyecto o global, zoom, vacaciones, botón de exportar)
    my-tasks-view.js                  "Mis tareas": tabla ordenable (proyecto + personales), con selección múltiple y su barra de acciones masivas, igual que Lista
    archive-view.js                   Proyectos archivados
    metrics-view.js                   Panel de métricas (solo administradores) — resumen, carga por persona y vencidas, todo de tareas de proyecto
  app.js                     Conecta todo: sesión, estado, enrutado simple, suscripción a notificaciones
firestore.rules             Reglas de seguridad de Firestore
```

## 5. Modelo de datos (Firestore)

- **`users/{uid}`** — `name`, `email`, `role` (`admin` | `miembro`), `deleted` (`true` una vez eliminada desde "Administrar equipo" — apartado 3; ausente o `false` en una cuenta activa), `theme` (`"dark"` | `"light"` | `"classic"` — opcional; sin definir se trata como `"dark"`), `personalCustomFieldDefs[]` (mismo formato que los de proyecto, pero solo tuyos — se usan en "Mis tareas"), `columnPrefs` (`{[scopeKey]: {widths:{[colKey]:px}, hidden:[colKey,...], sort:{column,direction}}}`, `scopeKey` = `project:<id>` o `mytasks` — ancho, columnas ocultas y orden de filas de las tablas, por ámbito), `columnOrder[]` (claves de columna en el orden elegido — a diferencia de `columnPrefs`, este es único y global: el mismo orden se aplica en Mis tareas y en cualquier proyecto; una clave ausente aquí se coloca al final). Un perfil "ficticio" creado por el importador de Asana añade además `isImported: true`, `asanaGid` (id de esa persona en Asana) y `mergedInto` (uid real una vez se le aplica una equivalencia; `null` mientras sigue ficticio) — su `{uid}` no es un UID de Firebase Auth real, sino `asana:<gid>`
- **`projects/{id}`** — `name`, `description`, `color`, `icon` (emoji; `📁` si no se ha elegido uno), `sections[]`, `memberIds[]` (informativo), `customFieldDefs[]` (`{id,name,type:'lista'|'numero'|'texto',options[]}`), `archived`, `createdBy`. Si viene de una importación, además `asanaGid`
- **`tasks/{id}`** — `projectId` (proyecto **principal**; null si no tiene ninguno), `sectionId` (su sección dentro de ese proyecto principal), `extraProjectIds[]` (proyectos **adicionales**, más allá del principal — una tarea puede estar en varios proyectos a la vez, ver el apartado 3), `extraSections` (`{[projectId]: sectionId}` — su sección dentro de cada proyecto adicional; solo tiene sentido si `projectId` ya está relleno, nunca al revés), `ownerId` (quien creó la tarea como personal en algún momento — se conserva aunque después se le añadan proyectos, como respaldo de permiso de borrado y para saber "esto nació como personal" incluso si ya no lo es del todo), `title` (texto plano — una parte entre `**dobles asteriscos**` se muestra en negrita, apartado 3; nunca HTML de verdad, a diferencia de `description`), `description` (HTML del editor enriquecido; texto plano en tareas antiguas que no se han vuelto a editar), `assigneeIds[]` (en una tarea sin ningún proyecto, quien conste aquí también puede leerla y editarla desde su propia "Mis tareas", no solo `ownerId` — ver `firestore.rules` y el apartado 3), `startDate`, `dueDate`, `priority`, `tags[]` (nombres; el color vive en `tags/`), `dependsOn[]` (dato heredado — ya no se edita desde la interfaz, ver apartado 7), `subtasks[]`, `attachments[]` (`{id,name,url}`), `customFields` (`{[fieldId]: valor}`), `isComplete`, `isMilestone`, `order` (único y global por tarea, no por proyecto — ver apartado 7). Si viene de una importación, además `asanaGid`
- **`tasks/{id}/comments/{id}`** — `authorId`, `authorName`, `text`. Si viene de una importación, además `asanaGid`; si llegó por combinar tareas duplicadas, además `mergedFrom` (título de la tarea original de la que venía)
- **`tags/{slug}`** — `name`, `color`
- **`notifications/{id}`** — `userId` (destinataria — la única que la lee), `fromUserId`, `fromName`, `type` (siempre `"task_assigned"` por ahora — el modelo deja sitio a más tipos en el futuro, apartado 6), `taskId`, `taskTitle` (copia del título en el momento de crearse — puede traer sus propias marcas de negrita, apartado 3), `projectId` (o `null`, tarea personal), `read` (bool), `createdAt`
- **`meta/asanaUserMap`** — `{[gidDeAsana]: uidReal}`: la tabla de equivalencias del importador de Asana. Quien no aparezca aquí sigue siendo un usuario ficticio
- **`meta/asanaImportIndex`** — `{projects:{[gid]:id}, tasks:{[gid]:id}, comments:{[gid]:{taskId,commentId}}}`: de qué se ha importado ya, para que repetir una importación no duplique nada y se pueda deshacer una prueba de un tirón

## 6. Qué falta (próxima iteración)

- Vistas guardadas de verdad (nombrar y guardar una combinación de filtros para reutilizarla — ahora mismo el buscador trae unas cuantas ya hechas, pero no se pueden crear personalizadas)
- Automatizaciones, formularios de solicitud, revisión de archivos, metas/OKRs, integraciones
- Convertir texto de la descripción directamente en una subtarea enlazada (como el botón "Crear tarea" del editor de Asana) — el editor de texto enriquecido no lo trae todavía; encaja mejor como una función aparte, con su propio diseño.
- Eliminar una cuenta de Firebase Authentication por completo (que ya no ocupe ni un hueco en la lista de usuarios de la consola de Firebase): sigue sin poder hacerse desde el navegador (hace falta el SDK de administración de Firebase, con un backend) — de momento no está implementado. Desde la v35 sí se le puede quitar el acceso a Chusy de verdad (apartado 3, "Eliminar (o reactivar) una cuenta"), que para el caso de uso habitual — alguien que deja el equipo — cubre lo mismo sin necesitar ese backend.
- Selección múltiple y barra de acciones masivas en Tablero (ya está en Lista y en Mis tareas)
- Más tipos de notificación además de "te asignaron una tarea" (comentario nuevo, fecha próxima a vencer...) — el modelo ya deja sitio para ello (`notifications/{id}.type`, ver apartado 5), pero de momento solo existe ese primer tipo

## 7. Limitaciones conocidas

- La primera vez que Firestore ejecute una consulta con una combinación de campos nueva (un `where` combinado con otro que no se haya usado juntos antes) puede pedir crear un índice compuesto, con un enlace en la consola del navegador que lo rellena todo solo — hay que pulsar "Crear índice" y esperar a que pase de "Compilando" a "Habilitado" (un minuto o dos, con datos de este tamaño). No es algo que pase "una vez y ya está" para siempre: cada combinación de campos distinta (aunque se parezca a una que ya tenga su índice) necesita el suyo propio — por ejemplo, el índice para `assigneeIds` + `projectId` de la v25→v26 no sirve para el de `extraProjectIds` + `projectId` que hizo falta en la v30, aunque las dos consultas tengan la misma forma. Mientras el índice pendiente no esté listo, esa consulta en concreto falla — desde la v31, un fallo así ya no deja toda la vista en blanco (ver ese mismo historial), pero los resultados que dependían de esa consulta no aparecerán hasta que el índice se termine de crear.
- El orden de tareas al arrastrar en el tablero usa valores numéricos intermedios; a gran escala convendría "renormalizar" los números de vez en cuando (no es un problema al tamaño de un departamento). Ese mismo campo (`order`) es único y global por tarea, no por proyecto: una tarea en varios proyectos a la vez comparte el mismo valor de orden en los tableros de todos ellos, así que arrastrarla en el tablero de un proyecto puede desplazar ligeramente su posición relativa en el de otro (secciones distintas, así que nunca se mezcla con las tareas "equivocadas" — solo afecta al orden exacto dentro de cada una). No se ha hecho un `order` por proyecto a propósito, para no complicar el modelo por un efecto que en la práctica apenas se nota al tamaño de un departamento.
- Los anchos de columna "de serie" son valores fijos pensados para el contenido habitual (fecha corta, una etiqueta de prioridad, unos pocos avatares), no una medición real del contenido de cada tarea — para eso están el arrastre y el ocultar/mostrar, que si ajustas una vez quedan guardados para ti.
- El importador de Asana no trae adjuntos, campos personalizados ni dependencias como dato estructurado, aunque el historial de actividad demuestre que los dos últimos sí se usaron en su momento. Para no perderlos en la importación definitiva, hay que pedirle esos campos a la API al generar el export; los adjuntos, mejor añadirlos aparte en su forma normal (un archivo que subir a donde vaya a vivir), no como enlace a Asana. Relacionado: una tarea personal importada sin responsable asignado en el propio Asana llega con `assigneeIds` vacío aunque sí tenga `ownerId` — el campo "Responsables" del modal (ahora visible también en tareas personales, ver apartado 3) no mostrará a nadie marcado hasta que se edite y se guarde una vez.
- Una vez aplicada una equivalencia (persona de Asana → cuenta real), cambiarla por otra cuenta distinta no está pensado para hacerse desde el panel — solo el primer paso de "ficticio → cuenta real" reescribe tareas y comentarios automáticamente.
- Combinar tareas duplicadas traslada los comentarios a la tarea que sobrevive, pero mover un comentario ajeno (que no escribiste tú) exige ser admin — la misma regla que usa el importador de Asana para reasignar autoría histórica. Si no eres admin y alguna de las tareas fusionadas tenía comentarios de otras personas, esos concretos no se trasladan: se quedan colgando en la tarea que se borra (igual que ya pasa hoy al borrar una tarea suelta desde el menú contextual, que tampoco limpia sus comentarios).
- El borrado masivo desde la barra de selección no es una única operación atómica: borra tarea por tarea, así que si seleccionas alguna de la que no eres ni su dueña ni admin, esa en concreto no se borra y se avisa de cuántas sí se pudieron eliminar.
- Si una tarea ya tenía guardada una dependencia ("bloqueada por") de cuando esa función todavía existía en la interfaz, el dato sigue en Firestore pero ya no hay manera de verlo ni de quitarlo desde ahí.
- El editor de descripción usa `document.execCommand` por debajo para negrita, listas, citas, etc. — la documentación de los navegadores lo marca como obsoleto, pero lo siguen soportando todos los navegadores actuales; si algún día alguno deja de hacerlo, ahí está la pista de por dónde mirar.
- Preferencias de cada persona que viven en Firestore (viajan entre dispositivos porque son de la cuenta, no de este navegador): el modo claro/oscuro (`users/{uid}.theme`) y las preferencias de columnas de las tablas — ancho, columnas ocultas y orden de filas (qué columna y en qué dirección, desde la v35) por ámbito, y el orden de las propias columnas (su posición), que en cambio es único y global (`columnPrefs` / `columnOrder`, ver apartados 3 y 5). Sobre esto último: `columnOrder` es un array de claves compartido entre Mis tareas y todos los proyectos; si más adelante se borra un campo personalizado que llegó a moverse alguna vez, su clave se queda huérfana ahí dentro — no causa ningún problema (`applyColumnOrder` simplemente la ignora, ya que no la encuentra entre las columnas de ningún contexto), pero tampoco se limpia sola.
- Preferencias que en cambio son de este navegador, no de la cuenta (no viajan a otro dispositivo): la barra lateral minimizada, los filtros recordados de "Mis tareas" (con "Pendiente" aplicado de serie si no hay nada guardado todavía), la última sección visitada al entrar (con "Mis tareas" de serie si no hay nada guardado, o si la sección guardada ya no existe) y el contador de "tareas completadas hoy" para la recompensa grande por racha del día — completar tareas repartido entre el ordenador y el móvil el mismo día no las suma en un único contador: cada uno lleva el suyo, y el hito puede saltar en un dispositivo sin haber saltado todavía en el otro.
- Desde la v33, `index.html` carga el CSS con un número de versión (`css/styles.css?v=N`), para que el navegador no se quede con una copia en caché de una entrega anterior sin darse cuenta de que hay una nueva — **subir ese número en cada entrega nueva** es ahora uno más de los pasos manuales de cada versión, junto con republicar `firestore.rules` cuando cambien y crear el índice compuesto si Firestore lo pide.
- La exportación a PDF de una línea de tiempo (v34) pagina sola cuando hay muchas tareas, pero si el salto de página cae en mitad de una sección/proyecto largo, su etiqueta no se repite arriba de la página siguiente — las tareas de después de ese salto quedan sin la referencia visual de a qué grupo pertenecen hasta volver a la página anterior. El Excel no tiene este problema (la sección va repetida en cada fila).
- La negrita del título (v35) usa una marca ligera, `**así**`, no un editor de verdad: un `**` que se quede sin su pareja de cierre se ve tal cual (como asteriscos normales, sin poner nada en negrita), y quien escriba tres seguidos (`***así***`) puede ver algún asterisco de más colándose dentro del texto en negrita. Casos raros y sin ninguna consecuencia grave si pasan — y sin ningún riesgo de seguridad en ningún caso, ver el historial de la v35 para el porqué.
- La plantilla Martech del Excel (v36, arreglada de fondo en la v38 — ver su historial) admite hasta 50 tareas con fecha y un rango de hasta 40 semanas dentro de un mismo año natural — los mismos límites que ya traía la plantilla original de la empresa. De esas 50, 19 ya vienen listas de fábrica en la plantilla (filas 13-23, 25 y 27-33 — se saltan la fila "OBSERVACIONES" y un hueco fijo); a partir de la 20ª, el código clona filas nuevas y extiende el formato condicional/validación/área de impresión él solo. Si un proyecto no cabe en alguno de los tres límites, se avisa por qué y se genera el Excel genérico en su lugar en vez de fallar sin más — la línea de tiempo global siempre usa este genérico, porque la plantilla da por hecho un único cliente/proyecto. Ampliar también las 40 columnas de semana haría falta reescribir a mano la cadena de fórmulas de fecha de la cabecera, algo que no se ha llegado a hacer todavía.
- Las notificaciones (v36) se autolimpian solas desde la v39 — máximo 20 por persona, ver su historial para el mecanismo exacto. Sigue habiendo un único tipo de aviso (`task_assigned`, campo `type` pensado para más en el futuro) y el panel no tiene manera de ver una notificación más antigua que las 20 visibles, ni falta que hace: al no poder pintarse ni abrirse nunca, se marca sola como leída y acaba borrándose.
- **Quién puede borrar una tarea** (`firestore.rules`, `allow delete` de `tasks`): quien la creó, quien figura como su `ownerId` (para una tarea personal, quien la creó de entrada), quien creó el proyecto al que pertenece, o un/a admin. Ser responsable de una tarea (estar en `assigneeIds`) da permiso para leerla, editarla y completarla, pero NO para borrarla — así que, por ejemplo, si le asignas a alguien una tarea personal tuya, esa persona puede trabajarla pero no eliminarla; solo tú (o un/a admin) podéis. Es una decisión de diseño, no un bug, pero está sin confirmar del todo — ver el historial de la v40 (de donde salió esto, al investigar un aviso de "Tarea eliminada" que no se había eliminado de verdad) para las dos frases exactas que haría falta cambiar en `firestore.rules` si se decide ampliarlo a quienes sean responsables.
